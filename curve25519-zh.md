# 实现 Curve25519/X25519：椭圆曲线密码学教程

Martin Kleppmann，剑桥大学，英国

许多教科书涵盖了椭圆曲线密码学背后的概念，但很少解释如何从方程出发，实现一个可运行的、快速且安全的实现。另一方面，虽然许多密码学库的代码以开源形式提供，但对于未经训练的人来说可能相当晦涩，而且很少附有详细文档来解释代码的由来及其正确性。本教程弥合了椭圆曲线密码学中数学与实现之间的鸿沟。它面向密码学初学者，假设读者仅有很少的数学背景。本文从基本原理出发，展示如何推导 X25519 Diffie-Hellman 密钥协商方案中每一行代码的实现过程，该方案基于 Curve25519 椭圆曲线。该实现既快速又安全；特别地，它采用常数时间算法以防止侧信道攻击。

CCS 概念：• 安全与隐私 → 公钥加密；• 计算理论 → 密码学原语。

关键词与短语：椭圆曲线密码学、Diffie-Hellman 密钥协商、密码学算法实现、常数时间算法、侧信道攻击防护

# 1 引言

Curve25519 [3] 是一种应用非常广泛的椭圆曲线：它被用于 X25519 标准 [24] 中的 Diffie-Hellman 密钥协商，而 X25519 是 TLS 1.3 [29] 中的强制算法，保护着全球浏览器中大量的 HTTPS 连接。它还被用于 WhatsApp [30]、Signal [26] 以及其他许多系统和协议 [21] 中的加密。然而，椭圆曲线密码学的标准教科书 [11, 13, 20, 23] 早于 Curve25519 出版，目前并没有太多好的资源来解释该算法的实际实现是如何工作的。查看 X25519 的实现（例如参见第 28 页的代码清单 5，或 X25519 标准文档 [24] 的第 5 节），你会发现一连串神秘的算术运算，几乎没有注释、解释或论证。

本教程的目标是通过仔细分析 X25519 某一特定实现的每一行代码，来介绍椭圆曲线密码学。我们展示如何从基本原理推导出算法，逐步讲解代数推导过程，并论证其正确性。不需要高等数学背景：所需的只是一些基本的模运算知识，这在大多数本科计算机科学课程中都会涵盖。

我们分析的实现基于 TweetNaCl [9, 10]，这是一个小型但实用的密码学库，其 API 与 NaCl [7, 8] 相同。其名称来源于该实现可以容纳在 100 条每条最多 140 个字符的推文中。TweetNaCl 最初用 C 语言编写，但其简洁性使其被广泛移植到各种其他语言，如 JavaScript [12]。尽管简洁，TweetNaCl 具有我们对成熟密码学库所期望的强大安全属性：特别地，它使用常数时间算法来防止侧信道攻击（即不基于秘密值进行分支或数组查找）。与原始的 TweetNaCl 实现 [10] 相比，本文中的代码经过了轻微的重新格式化和简化以提高可读性，同时保持功能、安全性和性能不变（变更细节见附录 A）。

作者地址：Martin Kleppmann, mk428@cst.cam.ac.uk, University of Cambridge, Department of Computer Science and Technology, 15 JJ Thomson Avenue, Cambridge, CB3 0FD, United Kingdom.

TweetNaCl 宣称自己是"可审计的" [9]，意思是其代码足够简短和简单，可以通过代码审查来确认其正确性。然而，据我所知，没有任何此类审计的细节被公开发表。JavaScript 移植版确实经过了专业审计，但报告 [15] 没有涉及任何技术细节。之前对 NaCl/TweetNaCl 的分析 [4, 22] 对部分算法给出了论证，但也留下了许多细节未加解释。本文部分填补了这一空白：我们仅讨论 X25519，而不涉及 TweetNaCl 中出现的其他算法，如 Salsa20 流密码、Poly1305 认证器或 Ed25519 签名方案。

与包含经过形式化验证的 X25519 实现 [31] 的 HACL\* [1] 不同，本文的目标不在于验证 TweetNaCl 是否正确，而在于通过仔细研究某一特定算法及其实现，来教授椭圆曲线密码学的安全实现是如何工作的。

# 2 背景知识

本节简要介绍后续章节所需的数学工具和概念。

## 2.1 模运算

模 $p$ 的整数集为 $\mathbb{Z}_p = \{0, 1, \dots, p-1\} = [0, p-1]$。在密码学中，计算通常在模 $p$ 下进行。这意味着如果计算结果超出了 0 到 $p-1$ 的范围，我们通过加或减 $p$ 来使其"回绕"，直到得到一个在 0 到 $p-1$（含）之间的数。

例如，如果我们在模 7 下运算，则 $1 + 1 = 2, 2 + 1 = 3, \ldots, 5 + 1 = 6$，但 $6 + 1$ 会回绕到 0。我们将其写作 $6 + 1 \equiv 0 \pmod{7}$。这类似于 C 编程语言中的无符号整数溢出，例如无符号 32 位整数上的运算就是在模 $2^{32}$ 下进行的。

一般而言，我们说两个整数 $a, b \in \mathbb{Z}$ 在模 $p$ 下同余，写作 $a \equiv b \pmod{p}$，当且仅当存在 $k \in \mathbb{Z}$ 使得 $a - b = kp$。也就是说，我们可以通过反复加或减 $p$ 来在 $a$ 和 $b$ 之间转换。当通过加或减 $p$ 的倍数将一个数纳入 $[0, p-1]$ 范围时，我们称这个过程为模 $p$ 归约。

同余模 $p$ 的 ≡ 运算符在很多方面类似于等号。例如，如果 $a \equiv b \pmod{p}$，则 $a + c \equiv b + c \pmod{p}$，且 $a \cdot c \equiv b \cdot c \pmod{p}$。也就是说，如果两个表达式在模 $p$ 下同余，我们可以用一个表达式替换另一个。

这意味着当计算在模 $p$ 下进行时，我们可以在计算的每一步之后进行模 $p$ 归约。例如，假设我们要计算 $3 \cdot (5 + 6)$ 模 7。我们可以先计算 $5 + 6$ 并将其模 7 归约，即 $5 + 6 = 11 \equiv 4 \pmod{7}$，然后计算 $3 \cdot 4 = 12 \equiv 5 \pmod{7}$。其结果与直接计算 $3 \cdot (5 + 6) = 3 \cdot 11 = 33 = 4 \cdot 7 + 5 \equiv 5 \pmod{7}$ 相同。

模 $p$ 归约在计算机上执行计算时非常有用，因为它允许我们用固定数量的比特来表示值。例如，如果 $0 < p \leq 2^n$，则 $\mathbb{Z}_p$ 中的任何数都可以用 $n$ 个比特表示。

在密码学中，$p$ 通常是一个大素数。例如，Curve25519 使用模素数 $p = 2^{255} - 19 =$ `0x7fffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffed` 的算术（十六进制表示），因此曲线计算中的数可以用 255 个比特（不到 32 字节）表示。我们可以使用开源数学软件 SageMath 来检查 $p$ 确实是素数：

```python
(2^{255} - 19).is_prime()  # 返回 True
```

## 2.2 群

阿贝尔群是一个集合 $E$ 加上一个运算 •。该运算组合集合中的两个元素，记为 $a \bullet b$，其中 $a, b \in E$。此外，该运算必须满足以下要求：

**封闭性：** 对所有 $a, b \in E$，运算结果 $a \bullet b$ 也在 $E$ 中。

**交换律：** 对所有 $a, b \in E$，有 $a \bullet b = b \bullet a$。

**结合律：** 对所有 $a, b, c \in E$，有 $(a \bullet b) \bullet c = a \bullet (b \bullet c)$。

**单位元：** 存在一个元素 $e \in E$，称为单位元或中性元，使得对所有 $a \in E$ 有 $e \bullet a = a \bullet e = a$。

**逆元：** 对每个 $a \in E$，存在一个元素 $b \in E$ 使得 $a \bullet b = b \bullet a = e$，其中 $e$ 是单位元。我们称 $b$ 为 $a$ 的逆元，写作 $b = a^{-1}$，反之亦然 $(a = b^{-1})$。

非阿贝尔群具有除交换律之外的上述所有性质，但本文涉及的所有群都是阿贝尔群。$E$ 中元素的个数称为群的阶。群是一个非常有用的抽象，因为它具有许多良好的数学性质，特别是当 $E$ 中元素的个数是素数时（这称为素数阶群）。

构造群有许多种可能的方式。例如，（正负）整数集 $\mathbb{Z}$ 加上加法运算符 + 构成一个群，其单位元为 0，其中元素 $a \in \mathbb{Z}$ 的逆元为 $-a$。这个群是无限的，因为整数 $\mathbb{Z}$ 有无穷多个。

要构造有限群，我们可以使用 $\mathbb{Z}_p$（模 $p$ 的整数集），群运算为加法 + 后接模 $p$ 归约。这个群的单位元为 0，$a \in \mathbb{Z}_p$ 的逆元为 $p - a$。这种构造称为模 $p$ 整数加法群。

另一种有限群构造使用整数集 $\mathbb{Z}_p^* = \{1, \ldots, p-1\} = \mathbb{Z}_p \setminus \{0\}$ 作为元素，其中 $p$ 是素数，群运算为乘法 · 后接模 $p$ 归约。这个群称为模 $p$ 整数乘法群，其单位元为 1。并非显而易见的是，每个元素 $a \in \{1, \ldots, p-1\}$ 都有逆元（也称为乘法逆元），但当 $p$ 是素数时，这一事实可以利用 Bézout 恒等式来证明，许多数论教科书中都有涉及。在第 2.6 节中，我们将看到一个计算乘法逆元的算法。

最后，在第 4.2 节中，我们将使用椭圆曲线来构造另一种有限群。这个群构成了大多数椭圆曲线密码学算法的基础。

许多密码学算法和协议使用群时并不指定群应该如何实现。这是因为任何两个具有相同素数阶的群都是同构的：也就是说，一个群可以通过重命名元素来转换为另一个群。因此，非正式地说，两个具有相同素数阶的群"行为相同"，无论它们是如何实现的（尽管它们的安全性质可能不同）。这使得群的概念成为密码学中最常见和最有用的抽象之一。

## 2.3 Diffie-Hellman 密钥交换

群在密码学中的一个常见用途是 Diffie-Hellman 密钥交换，它允许两方（Alice 和 Bob）通过不安全的信道建立共享秘密，前提是攻击者只能观察而不能修改通信。这个共享秘密随后可以用作加密两方之间消息的密钥。如果攻击者可能主动干扰通信，则需要额外的认证，我们在此不讨论。

对于群元素 $g$ 和非负整数 $k$，我们将群运算符对 $g$ 的重复应用定义如下：

$$
g^{k} = \underbrace{g \bullet g \bullet \cdots \bullet g}_{k \text{ 次}} \quad \text{或递归地：} \quad g^{0} = e \quad \text{（单位元），且} \quad g^{k} = g \bullet g^{k-1} \quad \text{当 } k > 0。 \tag{1}
$$

我们在第 4.4 节中展示如何在 Curve25519 群中高效计算 $g^k$，即使 $k$ 很大。

假设 Bob 希望任何人都能向他发送加密消息，我们可以使用 Diffie-Hellman 来实现。Bob 必须首先生成一个密钥对，包括私钥和公钥，并将公钥公开。为了生成密钥对，Bob 选择一个随机整数 $j$ 作为私钥，并计算 $g^j$，其中 $g$ 是一个众所周知的群元素。这个群元素 $g$ 称为基点或生成元，我们稍后将看到它是如何选择的。群元素 $g^j$ 是 Bob 的公钥。

当 Alice 想要向 Bob 发送加密消息时，她获取 Bob 的公钥 $g^j$ 并选择一个随机整数 $k$。她计算 $(g^j)^k$ 并将结果用作对称密钥来加密她发给 Bob 的消息。她还计算 $g^k$ 并将这个群元素与消息一起发送给 Bob，而 $k$ 保持私密。当 Bob 从 Alice 收到 $g^k$ 时，他使用私钥 $j$ 来计算：

$$
(g^{k})^{j} = \underbrace{(\underbrace{g \bullet \cdots \bullet g}_{k \text{ 次}}) \bullet \cdots \bullet (\underbrace{g \bullet \cdots \bullet g}_{k \text{ 次}})}_{j \text{ 次}} = \underbrace{g \bullet \cdots \bullet g}_{j \cdot k \text{ 次}} = \underbrace{(\underbrace{g \bullet \cdots \bullet g}_{j \text{ 次}}) \bullet \cdots \bullet (\underbrace{g \bullet \cdots \bullet g}_{j \text{ 次}})}_{k \text{ 次}} = (g^{j})^{k}
$$

由于 • 的结合律，Bob 计算的群元素 $(g^k)^j$ 等于 Alice 用来加密消息的值 $(g^j)^k$。因此，Alice 和 Bob 获得了相同的共享秘密，Bob 可以解密 Alice 的消息。攻击者知道 $g$ 和 $g^j$（因为它们是公开的），并且可能通过窃听网络传输来获知 $g^k$。为了使 Diffie-Hellman 密钥交换是安全的，攻击者在已知 $g$、$g^j$ 和 $g^k$ 的情况下计算 $g^{jk}$ 必须是极其困难的。

## 2.4 群的安全性质

如果以下假设成立（其中 $i$、$j$ 和 $k$ 从 $\mathbb{Z}_p$ 中均匀随机选取，且 $p$ 足够大），则 Diffie-Hellman 协议是安全的：

**离散对数困难性：** 给定 $g$ 和 $g^k$，攻击者无法有效计算 $k$。

**计算性 Diffie-Hellman (CDH) 假设：** 给定 $g$、$g^j$ 和 $g^k$，攻击者无法有效计算 $g^{jk}$。

**判定性 Diffie-Hellman (DDH) 假设：** 攻击者被随机（等概率）给予两个元组之一：$(g, g^j, g^k, g^{jk})$ 或 $(g, g^j, g^k, g^i)$。然后攻击者必须判断它被给予的是包含 $g^{jk}$ 的元组还是包含随机群元素 $g^i$ 的元组。DDH 假设指出，攻击者以显著大于 $\frac{1}{2}$（随机猜测）的概率选择正确答案是不可行的。

如果攻击者能计算离散对数，他们可以从 $g^j$ 和 $g^k$ 计算出 $j$ 和/或 $k$，从而计算出 $g^{jk}$；因此，假设 CDH 意味着假设离散对数是困难的。此外，如果攻击者能计算 $g^{jk}$，那么他们就能区分 $g^{jk}$ 和 $g^i$，因此假设 DDH 意味着假设 CDH。然而，反过来不一定成立：例如，可能直接从 $g^j$ 和 $g^k$ 计算 $g^{jk}$，而无需先计算 $j$ 或 $k$，尽管目前尚不知道有这样的算法。

第 2.2 节中的群在这些安全性质方面表现如何？

- 在模 $p$ 整数加法群中，离散对数很容易计算，因此这个群不安全。在这个群中计算 $g^k$ 意味着将 $g$ 加自身 $k$ 次，这等同于计算乘积 $g \cdot k$。我们可以计算 $g$ 的乘法逆元（第 2.6 节），然后计算 $g^{-1} \cdot g \cdot k = k$ 来高效恢复 $k$。

- 在 $\mathbb{Z}_p^*$（模 $p$ 整数乘法群）中，离散对数被认为是困难的，但 DDH 假设不成立。详细解释超出了本文范围，但可以简述如下：攻击者可以利用 Euler 判别准则来确定 $g^k$ 是否为模 $p$ 的二次剩余，这会泄露 $k$ 的最低有效位（即 $k$ 是奇数还是偶数）。此外，$jk$ 是偶数当且仅当 $j$ 和/或 $k$ 是偶数。因此，如果攻击者被给予 $(g, g^j, g^k, g^i)$，且 $i$ 是奇数而 $j$ 或 $k$ 是偶数（或 $i$ 是偶数而 $j$ 和 $k$ 都是奇数），那么攻击者知道元组的第四个元素一定是随机群元素，而不是 $g^{jk}$。这给了攻击者相对于随机猜测的显著优势，因此 DDH 假设不成立。可以通过构造 $\mathbb{Z}_p^*$ 的素数阶子群来解决这个问题；例如，数字签名算法（DSA）就是这样做的。在这个子群中，DDH 假设被认为是成立的。然而，仍然存在的问题是，为了安全起见，数字必须相当大：如果我们希望计算离散对数的难度与破解 128 位对称密码相当，那么 $p$ 需要超过 3,000 位长。

- 在我们在第 4.2 节中构造的椭圆曲线（EC）群中，DDH 假设被认为是成立的。此外，在 EC 群中计算离散对数被认为比在相同大小数字的 $\mathbb{Z}_p^*$ 子群中更困难，因此 EC 群可以使用 256 位数字来达到与 3,000 位 $\mathbb{Z}_p^*$ 群相同的 128 位安全级别。这意味着 EC 群元素在网络数据包中占用更少空间，而且 EC 算法在给定安全级别下更快。这是使用椭圆曲线进行密码学而非 RSA 或 $\mathbb{Z}_p^*$ 子群的主要优势。

在所有这些情况下，DDH 假设对群成立是一个猜想，而非已证明的事实。我们尚未找到针对这些密码系统的有效攻击，这一事实并不能保证这样的攻击不存在。然而，这是目前我们所拥有的最好的方案。

## 2.5 有限域

椭圆曲线密码学背后的大多数计算都在有限域中进行。基于第 2.2 节中阿贝尔群的定义，域是一个元素集合 $F$ 加上两个运算（加法 + 和乘法 $a \cdot b$），具有以下性质：

- 集合 $F$ 和加法运算符 + 构成一个阿贝尔群，其单位元为 0。我们将 $a \in F$ 在此群中的逆元记为 $-a$。减法运算符 $a - b$ 是 $a + (-b)$ 的简写。

- 集合 $F \setminus \{0\}$ 和乘法运算符 · 构成一个阿贝尔群，其单位元为 1。（0 被排除是因为它没有乘法逆元，即不存在 $a \in F$ 使得 $0 \cdot a = 1$。）我们将 $a$ 在此群中的逆元记为 $a^{-1}$。除法运算符 $\frac{a}{b}$ 是 $a \cdot (b^{-1})$ 的简写。

- 加法和乘法运算符满足分配律：$a \cdot (b + c) = (a \cdot b) + (a \cdot c)$。

实数 $\mathbb{R}$ 加上通常的加法和乘法运算符构成一个具有无限元素集合的域，但我们也可以定义一个元素集合 $F$ 有限的域。这称为有限域（Galois 域）。

Curve25519 使用模素数 $p = 2^{255} - 19$ 的有限域。如第 2.2 节所述，该域的元素为 $\mathbb{Z}_p = \{0, 1, \ldots, p-1\}$，具有通常的整数加法和乘法运算，后接模 $p$ 归约。$a$ 模 $p$ 的加法逆元为 $-a = p - a$，它总是存在的。我们在第 2.6 节讨论乘法逆元。

在域中，我们可以像在实数 $\mathbb{R}$ 中一样对表达式进行代数操作，例如求解方程：加法、减法、乘法和除法都按预期工作。指数运算定义为重复乘法，如 (1) 式所示。注意在有限整数域中，模 $p$ 除法是乘以乘法逆元的简写：$\frac{a}{b} = a \cdot (b^{-1})$。因此，除法的结果仍然是模 $p$ 的整数（域元素），而不是分数。

## 2.6 循环群与乘法逆元

设 $a \in E$ 是有限群 $E$ 的一个元素，群运算为 •，单位元为 $e$。考虑 $a$ 的幂集，即 $\{a^0, a^1, \ldots\}$，其中 $a^0 = e$，$a^k = a \bullet a \bullet \cdots \bullet a$ 表示将 $a$ 对自身运算 $k$ 次。由封闭性可知，• 的结果总是群的元素，因此幂集必须是群元素 $E$ 的子集。当群有限时，幂集也必须是有限的。

$a \in E$ 的幂集称为由 $a$ 生成的 $E$ 的子群。（子群是群元素的一个子集，使得运算仍然满足第 2.2 节中列出的五个群性质。）群元素 $a$ 的阶定义为由 $a$ 生成的子群中的元素个数（类似于群的阶，即群中的元素个数）。

特别地，一种可能性是 $\{a^0, a^1, \ldots\} = E$：即 $a$ 生成整个群 $E$，因此 $a$ 的阶与群的阶相同。如果存在这样的群元素 $a$，则该群称为循环群，$a$ 称为该群的生成元。之所以称为"循环"，是因为如果你考察群元素序列 $a^0, a^1, a^2$ 等，由于群是有限的，该序列最终必然重复；在循环群中，该重复循环包含所有群元素，因此 $a^0 = a^{|E|}, a^1 = a^{|E|+1}, a^2 = a^{|E|+2}$，依此类推，其中 $|E|$ 是 $E$ 中的元素个数。

可以证明，如果群的阶 $|E|$ 是素数，则该群总是循环的。循环群的一个良好性质是，生成元给出了模 $|E|$ 整数与群元素 $E$ 之间的一一映射。因此，如果我们均匀随机选择一个整数 $k \in [0, |E|-1]$ 并计算 $a^k \in E$，结果是一个均匀随机选择的群元素——即每个群元素被选中的概率相等，均为 $\frac{1}{|E|}$。这一事实被用于各种密码学协议中。

在具有生成元 $a$ 和单位元 $e$ 的有限循环群中，我们有 $e = a^0 = a^{|E|} = a \bullet a^{|E|-1}$，因此 $a^{|E|-1}$ 必须是 $a$ 的逆元。一般而言，对于阶为 $k$ 的任何群元素 $a$，我们有 $a^k = e$。

类似的规则适用于 $\mathbb{Z}_p^*$（模 $p$ 整数乘法群）。如果 $p$ 是素数，则该群的阶为 $p-1$，因为 0 不是群的元素。对于任何 $a \in \{1, \ldots, p-1\}$，$a$ 模 $p$ 的乘法逆元为 $a^{-1} = a^{p-2}$（如前所述，指数为群阶减 1）。这源于费马小定理，该定理指出当 $p$ 是素数且 $a \not\equiv 0 \pmod{p}$ 时，$a^{p-1} \equiv 1 \pmod{p}$。由于 $a^{p-1} = a \cdot a^{p-2}$，我们有 $a^{p-2}$ 是 $a$ 模 $p$ 的乘法逆元。我们将在第 3.3 节中使用这一事实来计算逆元。

也可以使用扩展 Euclid 算法来计算乘法逆元。然而，这种方法通常不是常数时间的，而使用费马小定理的方法很容易实现为常数时间。

## 2.7 余因子与小子群

在第 4.2 节中，我们将使用 Curve25519 椭圆曲线定义一个群。该群的阶为 $hq$，其中 $h = 8$，$q = 2^{252} + 27742317777372353535851937790883648493$ 是素数。$q$ 仅略大于 $2^{252}$，从十六进制表示可以看出：$q$ = 0x1000000000000000000000000000000014def9dea2f79cd65812631a5cf5d3ed。Curve25519 的参数专门选择使得群的阶是一个小整数 $h$（称为余因子，此处为 8）与一个略大于 2 的大幂次的素数 $q$ 的乘积 [3]。

Lagrange 定理指出，如果群 $E$ 有一个子群 $E'$，则 $E$ 的阶可被 $E'$ 的阶整除。由于群元素的幂生成一个子群，这意味着任何群元素的阶必须是群阶的因子。因此，每个 Curve25519 群元素的阶必须是 1、2、4、8、$q$、$2q$、$4q$ 或 $8q$ 之一：这些是 $8q$ 的所有因子，因此也是群元素所有可能的阶。

如果群具有素数阶（即余因子 $h = 1$），群会更简单，因为那样所有群元素的阶要么是 1 要么是 $q$。然而，使用 Montgomery 曲线（如 Curve25519，见第 4 节）的椭圆曲线群总是具有 4 的倍数的余因子。由于 Montgomery 曲线有其他优势，Curve25519 的设计决策是接受 8 的余因子，即使这使得群的安全使用更加复杂 [19]。

出于 Diffie-Hellman 的目的（第 2.3 节），Curve25519 使用阶为 $q$ 的群元素作为生成元 $g$（基点）；由于该元素生成的子群具有素数阶，它是循环的。然而，如果 Alice 是恶意的，她可以不发送群元素 $g^k$ 给 Bob，而是发送一个具有小阶的群元素 $s$，例如阶为 8。当 Bob 随后计算 $s^j$（其中 $j$ 是 Bob 的私钥）时，结果是仅 8 个可能的群元素之一，因为 $s$ 生成一个阶为 8 的子群，所以 $s^a = s^{a+8} = s^{a+16} = \ldots$。根据 Bob 解密消息的方式，Alice 可以使用暴力方法检测 Bob 获得了这 8 个可能群元素中的哪一个，从而确定 Bob 私钥的最低三位。这称为小子群限制攻击。

防止这种攻击的一种方法是验证公钥 [2]：在接收到公钥 $s$ 时，接收方首先检查 $s$ 确实是群的元素，且不是单位元。此外，为了验证 $s$ 的阶为 $q$，接收方检查 $s^q = e$ 是否成立，其中 $e$ 是群的单位元。或者，接收方可以检查 $s^h \neq e$，其中 $h$ 是余因子，这之所以有效是因为当 $s$ 的阶小于 $q$ 时，$s^h = e$。

X25519 不执行公钥验证，而是使用不同的方法来防止小子群限制攻击：它确保指数始终是余因子 $h = 8$ 的倍数。因此，如果攻击者发送一个阶小于 $q$ 的群元素 $s$，则 $s^{hj}$ 的结果将始终是单位元，无论私钥 $j$ 是什么，因此攻击者对私钥一无所知。当使用阶为 $q$ 的正确生成元 $g$，且 $j \in [0, q-1]$ 均匀分布时，$g^{hj}$ 的分布与 $g^j$ 的均匀分布相同，因此指数中的额外因子 $h$ 是无害的。

以下是证明概要，表明因子 $h$ 不改变分布。设 $g$ 是素数阶 $q$ 的子群的生成元，$g^a$ 是该子群的任何元素，其中 $a \in [0, q-1]$ 均匀分布。假设 $h$ 不是 $q$ 的倍数，即 $\gcd(h, q) = 1$。则同余方程 $iq \equiv -a \pmod{h}$ 在 $[0, h-1]$ 中恰好有一个解 $i$。设 $i$ 为该解，则 $iq + a \equiv 0 \pmod{h}$，所以 $iq + a$ 是 $h$ 的倍数。此外，由于 $0 \leq i \leq h-1$ 且 $0 \leq a \leq q-1$，我们有 $0 \leq iq + a \leq hq - 1$，因此恰好存在一个 $j \in [0, q-1]$ 使得 $hj = iq + a$。由于 $g$ 生成的子群是循环的，我们有 $g^q = g^0$，因此 $g^{iq+a} = g^a$。因此，对于每个 $a \in [0, q-1]$，存在唯一的 $j \in [0, q-1]$ 使得 $g^a = g^{hj}$，反之亦然。因此，生成 $g^{hj}$ 的概率与生成 $g^a$ 的概率相同；由于概率分布是均匀的，该概率为 $\frac{1}{q}$。

# 3 有限域算术

在实现椭圆曲线运算之前，我们必须首先为底层有限域实现算术运算符（加法、减法、乘法和除法）。对于 Curve25519，这是模素数 $p = 2^{255} - 19$ 的整数域，如第 2.5 节所定义。

大多数编程语言没有内置对如此大数的支持；此外，通用的大数实现可能不适合密码学目的，因为运算的运行时间和内存访问模式可能取决于其输入值。时间和内存访问的这种变化可以被侧信道攻击利用，从而可能向攻击者泄露秘密。

本文分析的实现包含了自己对域算术的实现，它注意使用常数时间算法，其运行时间和内存访问模式不依赖于输入值。这些算法仅依赖于 C 语言中的标准算术和位运算符，它们总是花费相同的执行时间。

## 3.1 加法与减法

我们对模 $p = 2^{255} - 19$ 的整数使用两种表示：一个 32 元素的 8 位值（字节）数组，和一个 16 元素的 16 位值数组。32 字节表示用于输入和输出，而 16 元素表示在算术运算符内部使用。两种情况下都使用小端序，即数组的第一个元素包含最低有效位，最后一个元素包含最高有效位。

代码清单 1 展示了模 $p$ 的加法、减法和乘法的实现。第 3 行定义的 field_elem 数据类型用于 16 元素数的表示。尽管每个元素最初只持有 16 位，但它被声明为 64 位有符号整数的 16 元素数组，以简化后续计算。

我们可以将 16 元素数组 $(a_0, a_1, \ldots, a_{15})$ 看作通过将每个元素乘以适当的 2 的幂来表示一个数 $a$：

$$
a = a_0 2^0 + a_1 2^{16} + a_2 2^{32} + \dots + a_{15} 2^{240}
$$

即使单个元素 $a_i$ 超出 $[0, 2^{16} - 1]$ 的范围，这个表达式仍然是良定义的。

我们从代码清单 1 第 5-10 行的 unpack25519 函数开始，它将一个数（$\mathbb{Z}_p$ 域元素）从字节数组表示转换为 field_elem 表示。循环取两个相邻字节 in[2\*i] 和 in[2\*i + 1]，通过将第二个字节左移 8 位然后相加，将它们组合成一个 16 位值。在第 9 行，它强制最高有效位（第 255 位）为零，因为我们的数总是小于 $2^{255}$。严格来说，这个函数允许值在 $[0, 2^{255} - 1]$ 范围内，包括未模 $p$ 归约的值 $\{2^{255} - 19, \ldots, 2^{255} - 1\}$，但这不会造成任何损害，因为接受 field_elem 类型作为输入的函数能正确处理这些数。

代码清单 1 第 23-27 行的 fadd 函数将两个 field_elem 形式的域元素相加，类似地，第 29-33 行的 fsub 函数将两个域元素相减。这些函数很直接：它们只是分别对 16 个元素逐一相加或相减。

$$
\begin{array}{l}
a + b = (a_0 2^0 + a_1 2^{16} + a_2 2^{32} + \dots + a_{15} 2^{240}) + (b_0 2^0 + b_1 2^{16} + b_2 2^{32} + \dots + b_{15} 2^{240}) \\
= (a_0 + b_0) 2^0 + (a_1 + b_1) 2^{16} + (a_2 + b_2) 2^{32} + \dots + (a_{15} + b_{15}) 2^{240} \tag{2} \\
a - b = (a_0 2^0 + a_1 2^{16} + a_2 2^{32} + \dots + a_{15} 2^{240}) - (b_0 2^0 + b_1 2^{16} + b_2 2^{32} + \dots + b_{15} 2^{240}) \\
= (a_0 - b_0) 2^0 + (a_1 - b_1) 2^{16} + (a_2 - b_2) 2^{32} + \dots + (a_{15} - b_{15}) 2^{240} \tag{3}
\end{array}
$$

```c
typedef unsigned char u8;
typedef long long i64;
typedef i64 field_elem[16];

static void unpack25519(field_elem out, const u8 *in)
{
    int i;
    for (i = 0; i < 16; ++i) out[i] = in[2*i] + ((i64) in[2*i + 1] << 8);
    out[15] &= 0x7fff;
}

static void carry25519(field_elem elem)
{
    int i;
    i64 carry;
    for (i = 0; i < 16; ++i) {
    carry = elem[i] >> 16;
    elem[i] -= carry << 16;
    if (i < 15) elem[i + 1] += carry; else elem[0] += 38 * carry;
    }
}

static void fadd(field_elem out, const field_elem a, const field_elem b) /* out = a + b */
{
    int i;
    for (i = 0; i < 16; ++i) out[i] = a[i] + b[i];
}

static void fsub(field_elem out, const field_elem a, const field_elem b) /* out = a - b */
{
    int i;
    for (i = 0; i < 16; ++i) out[i] = a[i] - b[i];
}

static void fmul(field_elem out, const field_elem a, const field_elem b) /* out = a * b */
{
    i64 i, j, product[31];
    for (i = 0; i < 31; ++i) product[i] = 0;
    for (i = 0; i < 16; ++i) {
    for (j = 0; j < 16; ++j) product[i+j] += a[i] * b[j];
    }
    for (i = 0; i < 15; ++i) product[i] += 38 * product[i + 16];
    for (i = 0; i < 16; ++i) out[i] = product[i];
    carry25519(out);
    carry25519(out);
}
```

代码清单 1. 模 $p = 2^{255} - 19$ 的域算术：加法、减法和乘法。

由于 field_elem 类型对 16 个元素中的每一个都使用 64 位有符号整数，这些加法或减法运算不会溢出或下溢，我们不必担心进位传播。然而，我们必须记住，每个元素现在可能大于 $2^{16}$，或者为负数。

## 3.2 模 $p$ 乘法

代码清单 1 第 35-46 行的 fmul 函数将两个 field_elem 形式的数相乘。product 是一个 64 位整数的 31 元素数组，初始化为零。在第 39-41 行，我们在嵌套循环中遍历输入数 a 和 b 的每个元素，并将它们的乘积加到 product 的适当元素上。这相当于我们用纸笔进行长乘法的方式：

$$
\begin{array}{l}
\text{product} = a \cdot b = (a_0 2^0 + a_1 2^{16} + a_2 2^{32} + \dots + a_{15} 2^{240}) \cdot (b_0 2^0 + b_1 2^{16} + b_2 2^{32} + \dots + b_{15} 2^{240}) \\
= a_0 b_0 2^{0+0} + a_1 b_0 2^{16+0} + \dots + a_{15} b_0 2^{240+0} + a_0 b_1 2^{0+16} + a_1 b_1 2^{16+16} + \dots + a_{15} b_{15} 2^{240+240} \\
= a_0 b_0 2^0 + (a_1 b_0 + a_0 b_1) 2^{16} + (a_2 b_0 + a_1 b_1 + a_0 b_2) 2^{32} + \dots + a_{15} b_{15} 2^{480} \tag{4}
\end{array}
$$

此时 product 包含 a 和 b 的乘积（两个 255 位数的乘积是一个 510 位数）。

为了将 31 元素数组 product 转换为 16 元素的 field_elem 形式，我们可以模 $p$ 归约，如第 2.1 节所述。然而，出于性能原因，我们在乘法函数 fmul 中不完全模 $p$ 归约。取而代之的是，我们在代码清单 1 的第 42 行模 $2p = 2(2^{255} - 19) = 2^{256} - 38$ 归约。这是有效的，因为模 $p$ 的倍数归约保留了所有必要信息；我们稍后可以模 $p$ 归约，最终结果将与不进行中间模 $2p$ 归约时相同。

`for (i = 0; i < 15; ++i) product[i] += 38 * product[i+16]` 几乎将 product 模 $2p$ 归约，因为 $2^{256} = 2p + 38$：

$$
\begin{array}{l}
\text{product} = t_0 2^0 + t_1 2^{16} + t_2 2^{32} + \dots + t_{15} 2^{240} + t_{16} 2^{256} + t_{17} 2^{272} \dots + t_{30} 2^{480} \\
= t_0 2^0 + t_1 2^{16} + t_2 2^{32} + \dots + t_{15} 2^{240} + t_{16} 2^0 (2p + 38) + t_{17} 2^{16} (2p + 38) + \dots + t_{30} 2^{224} (2p + 38) \\
\equiv t_0 2^0 + t_1 2^{16} + t_2 2^{32} + \dots + t_{15} 2^{240} + 38 t_{16} 2^0 + 38 t_{17} 2^{16} \dots + 38 t_{30} 2^{224} \pmod{2p} \\
= (t_0 + 38 t_{16}) 2^0 + (t_1 + 38 t_{17}) 2^{16} + \dots + (t_{14} + 38 t_{30}) 2^{224} + t_{15} 2^{240} \tag{5}
\end{array}
$$

此步骤之后，结果在元素 product[0] 到 product[15] 中，我们忽略 product[16] 到 product[30]。这个计算"几乎"是模 $2p$ 归约，因为虽然数现在适合 16 元素的 field_elem 类型，但我们尚未做任何事情将每个元素纳入 $[0, 2^{16} - 1]$ 范围，因此数整体上并未完全模 $2p$ 归约。

在继续之前，我们应该检查到目前为止的计算不会溢出我们使用的 64 位变量（有符号整数溢出在 C 中是未定义行为，因此确保它不会发生很重要）。当我们在第 4.6 节中使用加法、减法和乘法函数时，结果将表明乘法的结果在成为另一次乘法的输入之前，最多经历一次加法或减法。因此，如果我们假设乘法返回的 field_elem 数中每个元素在 $[0, 2^{16}]$ 范围内，则在一次加法或减法之后，我们可以假设乘法输入的元素 $a_i, b_i$ 在 $[-2^{16}, 2^{17}]$ 范围内。方程 (4) 中一个元素被加最多项数的是 $t_{15}$：

$$
t_{15} = a_{15} b_0 + a_{14} b_1 + a_{13} b_2 + \dots + a_1 b_{14} + a_0 b_{15}
$$

每个乘积 $a_i b_j$ 在 $[-2^{33}, 2^{34}]$ 范围内，因此 16 个这样的项之和必须在 $[-2^{37}, 2^{38}]$ 范围内。方程 (5) 中的模 $2p$ 归约可能进一步将元素乘以因子 38；将 38 过近似为 $2^6$ 给出最终范围 $[-2^{43}, 2^{44}]$。因此，我们可以得出结论，64 位算术给我们提供了充足的空间来完成计算而不会溢出。

最后，为了将元素恢复到 $[0, 2^{16} - 1]$ 范围内，fmul 函数首先将 product[0] 到 product[15] 复制到输出变量 out 中，然后调用 carry25519 函数两次（第 43-45 行）。

carry25519 函数（代码清单 1 第 12-21 行）通过几乎将所有元素纳入 $[0, 2^{16} - 1]$ 范围来清理 field_elem 类型的值。我说"几乎"是因为在调用 carry25519 一两次之后，存在一些元素超出该范围的边界情况。为了确定所有元素都在 $[0, 2^{16} - 1]$ 内，该函数需要被调用三次。然而，在两次调用 carry25519 之后，该范围只能被超出很小的量；对于乘法函数的目的，两次调用就足够了，因为我们只需要确保元素值足够小，不会在成为后续乘法的输入时导致溢出。

carry25519 遍历 field_elem 数 elem 的 16 个元素，对每个元素执行以下操作：

(1) 第 17 行计算 `carry = elem[i] >> 16;`，选取所有高于低 16 位的比特。

(2) 第 18 行更新 `elem[i] -= carry << 16;`，从 elem[i] 中减去进位比特，使其值在 $[0, 2^{16} - 1]$ 范围内（即使 elem[i] 之前为负数的情况也是如此）。

(3) 在第 19 行，进位比特被加到下一个元素上，除非 elem[i] 已经是最后一个元素。如果我们处于最后一个元素（`i == 15`），进位乘以 38 后加到第一个元素上，如方程 (5) 中一样执行模 $2p$ 归约。尽管存在 if 语句，这个操作是常数时间的，因为它只依赖于变量 i，而 i 不是秘密。

如果不是最后一个元素的进位回绕到 elem[0]，carry25519 函数会将所有元素留在 $[0, 2^{16} - 1]$ 内。然而，这个进位可能导致 elem[0] 大于 $2^{16}$，甚至为负。第二次调用 carry25519 可以修复这个问题；然而，如果元素 elem[1] 到 elem[15] 接近 0xffff，elem[0] 的进位比特可能在第二次调用时引起级联进位，导致 elem[15] 到 elem[0] 的进一步进位，使 elem[0] 再次超过 0xffff。在第三次调用 carry25519 时，这种级联进位不再可能，因为中间元素的值现在为零，所以第三次调用最终将所有元素纳入 $[0, 2^{16} - 1]$ 范围。

无论如何，carry25519 被调用的次数必须是常数（不依赖于是否有进位），否则函数将不再是常数时间的。

## 3.3 计算乘法逆元

现在我们已经定义了加法、减法和乘法，域元素上缺失的算术运算是模 $p = 2^{255} - 19$ 的除法。如第 2.6 节所述，我们通过计算分母 $a$ 的乘法逆元 $a^{-1}$，然后将该逆元与分子 $b$ 相乘来执行除法 $\frac{b}{a}$。

代码清单 2 第 5-15 行的 finverse 函数计算其输入 `in` 的乘法逆元，将结果写入输出变量 `out`。输入和输出都是 field_elem 形式。逆元的计算使用第 2.6 节中的费马小定理，通过计算 $a^{-1} \equiv a^{p-2} \pmod{p}$。指数 $a^{p-2}$ 可以使用平方-乘法方法计算，该方法基于以下递推关系：

$$
a^{2i} = a^i \cdot a^i \qquad \text{和} \qquad a^{2i+1} = a \cdot a^i \cdot a^i
$$

也就是说，我们首先递归计算 $a^i$，然后对 $a^i$ 平方以获得 $a^{2i}$。如果指数是奇数，我们额外将结果乘以另一个 $a$ 以获得 $a^{2i+1}$。

$p - 2 = 2^{255} - 21 =$ 0x7fffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffeb 是常数，因此 finverse 硬编码了平方和乘法的模式。$p - 2$ 的所有比特都是 1，除了比特 2 和比特 4 为 0（其中比特 0 是最低有效位）。finverse 函数中的循环从最高有效位到最低有效位倒计数，对每个比特使用 fmul 函数对当前值 c 进行平方，并且对每个为 1 的比特还将 c 与输入值 `in` 相乘。尽管 $p - 2$ 包含 255 个比特，但循环能够从比特 253 开始，通过将 c 初始化为 `in`（而不是 1）来节省一次迭代。最后，c 被复制到输出变量 `out`。

```c
typedef unsigned char u8;
typedef long long i64;
typedef i64 field_elem[16];

static void finverse(field_elem out, const field_elem in)
{
    field_elem c;
    int i;
    for (i = 0; i < 16; ++i) c[i] = in[i];
    for (i = 253; i >= 0; i--) {
    fmul(c, c, c);
    if (i != 2 && i != 4) fmul(c, c, in);
    }
    for (i = 0; i < 16; ++i) out[i] = c[i];
}

static void swap25519(field_elem p, field_elem q, int bit)
{
    i64 t, i, c = ~(bit - 1);
    for (i = 0; i < 16; ++i) {
    t = c & (p[i] ^ q[i]);
    p[i] ^= t;
    q[i] ^= t;
    }
}

static void pack25519(u8 *out, const field_elem in)
{
    int i, j, carry;
    field_elem m, t;
    for (i = 0; i < 16; ++i) t[i] = in[i];
    carry25519(t); carry25519(t); carry25519(t);
    for (j = 0; j < 2; ++j) {
    m[0] = t[0] - 0xffed;
    for(i = 1; i < 15; i++) {
    m[i] = t[i] - 0xffff - ((m[i - 1] >> 16) & 1);
    m[i - 1] &= 0xffff;
    }
    m[15] = t[15] - 0x7fff - ((m[14] >> 16) & 1);
    carry = (m[15] >> 16) & 1;
    m[14] &= 0xffff;
    swap25519(t, m, 1 - carry);
    }
    for (i = 0; i < 16; ++i) {
    out[2*i] = t[i] & 0xff;
    out[2*i + 1] = t[i] >> 8;
    }
}
```

代码清单 2. 乘法逆元，以及从内部表示转换为字节数组。

## 3.4 转换回字节数组

有限域算术中我们需要完成的最后一个方面是将 field_elem 表示的数转换回字节数组表示，这由代码清单 2 第 27-48 行的 pack25519 函数完成。该函数执行第 3.1 节中讨论的 unpack25519 函数的逆操作。在此过程中，我们还将数模 $p$ 归约，这确保了模 $p$ 整数域中的每个不同元素都由唯一的字节串表示。这在我们稍后想要使用这样的域元素来派生加密密钥时至关重要。

首先，我们解释代码清单 2 第 17-25 行的辅助函数 swap25519(p, q, bit)。如果 bit 为 1，该函数交换参数 p 和 q 的内容（两者都是 field_elem 表示），如果 bit 为 0 则什么都不做。由于 bit 可能是秘密值的一部分，该函数不能使用简单的 if 语句，因为那不是常数时间的。相反，它必须确保无论 bit 的值如何，总是执行完全相同的操作。

swap25519 首先设置 `c = ~(bit - 1)`，当 bit == 0 时等于 0，当 bit == 1 时等于 0xffff...。接下来它遍历数的 16 个元素中的每一个，并对索引 i 计算 `t = c & (p[i] ^ q[i])`，当 bit == 0 时等于 0，当 bit == 1 时等于 p[i] ^ q[i]。因此操作 p[i] ^= t 在 bit == 0 时没有效果，在 bit == 1 时设置 `p[i] = p[i] ^ (p[i] ^ q[i]) = q[i]`。类似地，当 bit == 1 时 q[i] 被设置为 p[i]。

接下来，我们转向 pack25519。该函数首先将输入值 `in` 复制到 t，然后对它调用 carry25519 三次。如第 3.2 节所讨论的，这确保了 t 的所有 16 个元素都在 $[0, 2^{16} - 1]$ 范围内。因此，我们有一个尚未完全模 $2p$ 归约的 256 位数，因为它可能落在整个 $[0, 2^{256} - 1]$ 范围内（即它可能略大于 $2p$）。

为了将该数 $t$ 模 $p$ 归约，注意有三种可能：要么 $0 \leq t < p$（即 $t$ 已经模 $p$ 归约），要么 $p \leq t < 2p$（即我们需要从 $t$ 中减去 $p$ 以模 $p$ 归约），要么 $2p \leq t < 2^{256}$（即我们需要从 $t$ 中减去 $2p$）。然而，为了使函数是常数时间的，我们不能简单地检测这三种情况中的哪一种成立，然后减去适当的 $p$ 的倍数。相反，我们必须计算 $t - p$ 和 $t - 2p$，然后使用常数时间算法来选择三个值 $\{t, t - p, t - 2p\}$ 中返回哪一个。这由第 33-43 行的循环 `for (j = 0; j < 2; ++j)` 完成。

循环体的大部分被用于从 t 减去 $p$ 并将结果写入 m 的逻辑。该函数没有重用之前的 fsub 函数来减法，然后重复调用 carry25519 来处理进位，而是实现了自己的进位处理。

在第 34 行，`m[0] = t[0] - 0xffed;` 从 t[0] 中减去 $p$ 的最低 16 位，即 0xffed。这可能导致负数，但随后第 37 行的 `m[0] &= 0xffff;` 在先取进位比特 `(m[0] >> 16) & 1` 并从 m[1] 中减去它（第 36 行）之后，将其恢复到 $[0, 2^{16} - 1]$ 范围内。然后我们对 t 的接下来 14 个元素重复此操作，只是我们减去 0xffff（$p$ 的中间比特）而不是 0xffed。对于 t[15] 我们减去 0x7fff（$p$ 的最高 16 位），将进位比特放入变量 carry 中，并执行相同的步骤。

$t - p$ 结果的进位比特在结果为负时为 1，在结果为零或正时为 0。如果此比特为 1，第 42 行的 `1 - carry` 等于 0，因此 swap25519 什么都不做，变量 t 保持不变，负结果被丢弃。如果此比特为 0，`1 - carry` 等于 1，因此 swap25519 交换 t 和 m 的值，即将 t 更新为非负值 $t - p$。因此，$p$ 的减法仅在结果非负时生效。在两次循环迭代之后，$t$ 保证在 $[0, p-1]$ 范围内。

最后，在第 44-47 行，t 的值被复制到输出字节数组 out 中。每个 16 位元素被拆分为两个字节，并赋给字节数组的两个相邻元素。这完成了整个循环，从以字节数组形式到达的输入值开始，通过域算术运算以 field_elem 形式进行操作，最终将结果转换回字节数组。

# 4 椭圆曲线算术

Curve25519 使用曲线

$$
y^2 = x^3 + Ax^2 + x \tag{6}
$$

这称为 Montgomery 曲线，参数 $A = 486662$。我们将以方程 (6) 作为出发点；使用此方程和选择 $A$ 的论证见 Curve25519 论文 [3]。我们的推导对任何 $A^2 \neq 4$ 成立；此限制确保曲线具有所需的形状。其他一些曲线方程也用于椭圆曲线密码学，例如短 Weierstrass 方程

$$
y^2 = x^3 + Ax + B \tag{7}
$$

但本文我们聚焦于 Montgomery 曲线。

## 4.1 直线与椭圆曲线的交点

如果点 $P = (x, y)$ 是方程 (6) 的解，我们说该点在曲线上。图 1 展示了这样一条曲线的示例。注意曲线关于 $x$ 轴具有反射对称性；更正式地说，如果 $(x, y)$ 在曲线上，则 $(x, -y)$ 也在曲线上。这是因为变量 $y$ 仅出现在 (6) 的 $y^2$ 项中。

目前我们将坐标 $x$ 和 $y$ 视为实数。在 Curve25519 中它们实际上是模 $2^{255} - 19$ 的整数，但我们将使用实数进行以下推导，因为这更容易可视化，并允许我们使用一些微积分。最终结果可以适用于任何域。

如果我们有两个都在曲线上的点 $P = (x_P, y_P)$ 和 $Q = (x_Q, y_Q)$，我们可以通过这两个点画一条直线。如果我们假设 $x_P \neq x_Q$，则该直线与曲线相交于某个第三点 $R$，如图 1(a) 所示。我们将很快证明这个第三点 $R$ 总是存在。这条直线由方程定义

$$
y = \lambda x + c \quad \text{其中斜率为} \quad \lambda = \frac{y_Q - y_P}{x_Q - x_P} \quad \text{且} \quad y \text{ 截距为} \quad c = y_P - \lambda x_P \tag{8}
$$

![图 1](https://cdn-mineru.openxlab.org.cn/result/2026-06-03/4843056d-5136-4620-80c6-28bca21ee9d8/7be283abe9fdb5b8da7284571106c519bdd22e2ea0bb1314ead59aa3058caed0.jpg)

![图 1(b)](https://cdn-mineru.openxlab.org.cn/result/2026-06-03/4843056d-5136-4620-80c6-28bca21ee9d8/f1cb1a57d6f762449948d89dc7bb3dc168b8e3ab7dd4a8e409a59d31a1c2d79b.jpg)

图 1. (a) 如果我们在椭圆曲线上两个点 $P = (x_P, y_P)$ 和 $Q = (x_Q, y_Q)$ 之间画一条线，其中 $x_P \neq x_Q$，则该线与曲线再次相交于第三点 $R$。(b) 推广到 $P = Q$ 的情况，我们在 $P$ 处画曲线的切线，它与曲线相交于 $R$。

如果 $x_P = x_Q$，我们区分三种可能：$y_P = y_Q = 0$，或 $y_P = y_Q \neq 0$，或 $y_P = -y_Q \neq 0$。首先考虑 $y_P = -y_Q \neq 0$ 的情况，如图 2 所示：如果我们通过这两个点画一条直线，该线是垂直的，没有第三个交点。我们将在第 4.2 节中回到这种情况，以及 $y_P = y_Q = 0$ 的情况。

接下来，考虑 $y_P = y_Q \neq 0$ 的情况，即 $P = Q$ 且点不在 $x$ 轴上。在这种情况下，我们仍然可以定义通过 $P$ 和 $Q$ 的直线，我们选择线的斜率使其是曲线的切线（即它在 $P$ 处接触曲线但不穿过曲线）。这是斜率 $\lambda = (y_Q - y_P)/(x_Q - x_P)$ 在 $P$ 和 $Q$ 之间距离趋于零时的自然推广。

为了计算这条切线的斜率 $\lambda$，我们可以使用链式法则计算曲线方程 (6) 的导数：

$$
y^2 = x^3 + Ax^2 + x \quad \Longleftrightarrow \quad y = \pm \sqrt{x^3 + Ax^2 + x} \tag{9}
$$

$$
\lambda = \frac{\mathrm{d}y}{\mathrm{d}x} = \pm \frac{3x^2 + 2Ax + 1}{2\sqrt{x^3 + Ax^2 + x}} = \pm \frac{3x^2 + 2Ax + 1}{2|y|} = \frac{3x^2 + 2Ax + 1}{2y} \tag{10}
$$

方程 (10) 中 $\lambda$ 的符号对正负 $y$ 都正确。切线然后由 $y = \lambda x + c$ 如前定义，它在 $y \neq 0$ 时存在。如果 $y = 0$，切线是垂直的，我们将其作为第 4.2 节中 $y_P = -y_Q$ 情况的一部分处理。

![图 2](https://cdn-mineru.openxlab.org.cn/result/2026-06-03/4843056d-5136-4620-80c6-28bca21ee9d8/daaa3fb42fc6d75a7a0bef55c73271a0ac75c782f8f4b7c5fe9eb5f72817a127.jpg)

图 2. 如果 $x_P = x_Q$ 且 $y_P = -y_Q$，通过这两个点的线是垂直的。

在直线不垂直的情况下，我们计算直线与椭圆曲线相交的第三点 $R$。我们通过将直线方程 (8) 代入曲线方程 (6) 来完成：

$$
(\lambda x + c)^2 = x^3 + Ax^2 + x \quad \Longleftrightarrow \quad x^3 + (A - \lambda^2)x^2 + (1 - 2\lambda c)x - c^2 = 0 \tag{11}
$$

多项式 (11) 的根是直线与曲线交点的 $x$ 坐标。由于我们知道 $x_P$ 和 $x_Q$ 同时在直线和曲线上，$x_P$ 和 $x_Q$ 必须是 (11) 的根，因此我们可以将 (11) 除以多项式 $(x - x_P)(x - x_Q) = x^2 - (x_P + x_Q)x + x_P x_Q$。即使 $x_P = x_Q$ 这也有效，原因如下：在 $P = Q$ 的情况下，我们选择的线是曲线的切线；因此，(11) 的导数在 $x_P$ 处为零；因此，$x_P$ 是 (11) 的重根，我们可以将它除以 $x - x_P$ 两次。执行多项式除法：

$$
\begin{array}{l}
x + A - \lambda^2 + x_P + x_Q \\
x^2 - (x_P + x_Q)x + x_P x_Q \overline{) x^3 + (A - \lambda^2)x^2 + (1 - 2\lambda c)x - c^2} \\
x^3 - (x_P + x_Q)x^2 + x_P x_Q x \\
(A - \lambda^2 + x_P + x_Q)x^2 + (1 - 2\lambda c - x_P x_Q)x - c^2 \\
(A - \lambda^2 + x_P + x_Q)x^2 - (A - \lambda^2 + x_P + x_Q)(x_P + x_Q)x \\
+ (A - \lambda^2 + x_P + x_Q)x_P x_Q \\
\end{array}
$$

多项式除法产生了一个极其丑陋的余项表达式，但幸运的是我们不需要计算它，因为我们知道它必须为零。从商 $x + A - \lambda^2 + x_P + x_Q$ 我们得到第三交点 $R$ 的 $x$ 坐标：

$$
x_R = \lambda^2 - A - x_P - x_Q \tag{12}
$$

```python
# 模 p 的整数有限域（GF 是 Galois Field 的缩写）
field = GF(2^255 - 19)

# EllipticCurve(field, [a1, a2, a3, a4, a5]) 在给定域上构造一条椭圆曲线，
# 曲线方程为 y^2 + a1*x*y + a3*y = x^3 + a2*x^2 + a4*x + a5。
# 我们选择 a1 = 0, a2 = 486662, a3 = 0, a4 = 1, a5 = 0 以得到
# Montgomery 曲线方程 y^2 = x^3 + 486662 * x^2 + x。
E = EllipticCurve(field, [0, 486662, 0, 1, 0])

# 检查该曲线定义的群的阶（基数）
q = 2^252 + 27742317777372353535851937790883648493
q.is_prime() # 返回 True
E.cardinality() == 8 * q # 返回 True

# 定义基点（生成元）g 为 x 坐标 = 9 的点，并检查该点的阶
base = 9
g = [field(base), sqrt(field(base^3 + 486662 * base^2 + base))] # [x, y] 坐标
q * E(g) # 返回 (0 : 1 : 0)，即无穷远点
# 这表明点 g 在椭圆曲线群 E 中的阶为 q。
```

代码清单 3. 用 SageMath 计算 Curve25519 群的阶和基点。

我们将 $x_R$ 代入直线方程 (8) 来获得 $y$ 坐标：

$$
y_R = \lambda x_R + c = \lambda x_R + y_P - \lambda x_P = y_P + \lambda(x_R - x_P) \tag{13}
$$

由于 $(x_R, y_R)$ 在 $\lambda$ 存在时就有定义，我们知道第三交点 $R$ 在直线不垂直时总是存在。

## 4.2 构造群

我们现在将使用上一节的结果来构造一个群。群元素的集合是椭圆曲线 (6) 上的点集，加上一个特殊元素 ∞，我们称之为无穷远点：

$$
E = \{(x, y) \mid y^2 = x^3 + Ax^2 + x\} \cup \{\infty\} \tag{14}
$$

当 $x$ 和 $y$ 坐标是实数时，群有无限多个元素，但当它们是模 $p$ 的整数时，群的阶是有限的。对于 $x, y \in \mathbb{Z}_p$，有 $p^2$ 个可能的 $(x, y)$ 对，其中只有一些是曲线方程的解。解的精确数量取决于曲线方程、参数 $A$ 和底层域的大小。计算曲线点数的算法在教科书 [11, 13] 中有介绍。对于 $p = 2^{255} - 19$ 和 $A = 486662$ 的 Curve25519，如第 2.7 节所讨论的，我们有 $|E| = 8 \cdot (2^{252} + 27742317777372353535851937790883648493)$。我们可以使用 SageMath 来检查这是正确的，如代码清单 3 所示。

无穷远点没有坐标，其用途是处理垂直线。当两个不同的点具有相同的 $x$ 坐标且我们通过它们画一条垂直线时（如图 2），我们将无穷远点定义为直线"与曲线相交"的第三点。我们可以想象这个点位于 $y$ 轴上无穷远处，所有垂直线都与该点相交。

无穷远点还将作为我们群的单位元。也就是说，我们定义以下等式成立：

$$
P \bullet \infty = \infty \bullet P = P \quad \text{对所有 } P \in E \tag{15}
$$

特别地，∞ • ∞ = ∞。此外，对于曲线上的任何点 $P = (x_P, y_P)$，我们定义其逆元为 $P^{-1} = (x_P, -y_P)$，即通过关于 $x$ 轴镜像 $P$ 得到的点。根据定义，逆元满足以下性质：

$$
P \bullet P^{-1} = P^{-1} \bullet P = \infty \quad \text{对所有 } P \in E \tag{16}
$$

我们还定义 $\infty^{-1} = \infty$。

直觉上，我们可以将运算符 $P \bullet Q$ 看作通过画一条穿过 $P$ 和 $Q$ 的直线并找到曲线上的第三点来组合两个点。为了完整定义运算符 •，我们从以下思想开始：对于任何三个点 $P, Q, R \in E$，如果这些点在同一条直线上，则我们有

$$
P \bullet Q \bullet R = \infty \tag{17}
$$

我们可以通过定义 $P \bullet Q = R^{-1}$ 来实现这一点，然后 $(P \bullet Q) \bullet R = R^{-1} \bullet R = \infty$。也就是说，给定两个曲线点 $P$ 和 $Q$，我们可以画一条穿过这些点的直线，找到该线与曲线相交的第三点，然后通过取反其 $y$ 坐标来反转该点。以这种方式获得的点就是 $P \bullet Q$。

使用我们关于第三交点坐标的结果 (12) 和 (13)，以及斜率 $\lambda$ 的方程 (8) 和 (10)，我们现在可以为曲线上满足 $P_1 \neq P_2^{-1}$ 的任意两个点 $P_1$ 和 $P_2$ 定义 $P_1 \bullet P_2$：

$$
P_1 \bullet P_2 = (x_1, y_1) \bullet (x_2, y_2) = (x_3, y_3) \quad \text{其中}
$$

$$
x_3 = \lambda^2 - A - x_1 - x_2 = \left\{ \begin{array}{ll} \left(\frac{y_2 - y_1}{x_2 - x_1}\right)^2 - A - x_1 - x_2 & \text{若 } x_1 \neq x_2 \\ \left(\frac{3x_1^2 + 2Ax_1 + 1}{2y_1}\right)^2 - A - 2x_1 & \text{若 } x_1 = x_2 \end{array} \right. \tag{18}
$$

$$
y_3 = -(y_1 + \lambda(x_3 - x_1)) = \lambda(x_1 - \lambda^2 + A + x_1 + x_2) - y_1 = \lambda(2x_1 + x_2 + A) - \lambda^3 - y_1 = \tag{19}
$$

$$
= \left\{ \begin{array}{ll} \frac{(2x_1 + x_2 + A)(y_2 - y_1)}{x_2 - x_1} - \left(\frac{y_2 - y_1}{x_2 - x_1}\right)^3 - y_1 & \text{若 } x_1 \neq x_2 \\ \frac{(2x_1 + x_2 + A)(3x_1^2 + 2Ax_1 + 1)}{2y_1} - \left(\frac{3x_1^2 + 2Ax_1 + 1}{2y_1}\right)^3 - y_1 & \text{若 } x_1 = x_2 \end{array} \right.
$$

公式 (18) 和 (19)，连同定义 (15) 和 (16)，构成了 Montgomery 曲线的群律。这些定义可能看起来有些武断，但 • 必须这样定义才能得到一个群。例如，如果我们不反转直线的第三交点，结果运算将不构成群。

为了证明我们的定义构成一个阿贝尔群，我们需要证明第 2.2 节中的五个性质成立：

- 封闭性由定义成立，因为由 (18) 和 (19) 定义的点 $(x_3, y_3)$ 在曲线上，且 (15) 和 (16) 中群运算的结果也是 $E$ 的元素。

- 单位元 ∞ 存在且根据定义 (15) 具有所需的行为。

- 逆元 $P^{-1}$ 对每个 $P \in E$ 都存在且根据定义 (16) 具有所需的行为。

- 为了证明交换律成立，考虑几种情况。如果 $P = \infty$ 和/或 $Q = \infty$，由 (15) 我们有 $P \bullet Q = Q \bullet P$。如果 $P = Q^{-1}$，由 (16) 我们有 $P \bullet Q = Q \bullet P$。最后，如果 $P \neq \infty, Q \neq \infty$ 且 $P \neq Q^{-1}$，

考虑通过曲线点 $P$ 和 $Q$ 的直线。我们证明通过 $P$ 和 $Q$ 的线与通过 $Q$ 和 $P$ 的线相同，因此该线与曲线的第三交点必须相同。如果 $P = Q$，根据 (10) 两条线平凡地相同。如果 $P \neq Q$，我们考察直线方程 (8)：

$$
\begin{array}{l}
y = \lambda x + c = \lambda(x - x_P) + y_P = \frac{y_Q - y_P}{x_Q - x_P}(x - x_P) + y_P \tag{20} \\
= \frac{-(y_P - y_Q)}{-(x_P - x_Q)}(x - x_P) + \frac{y_P(x_P - x_Q)}{x_P - x_Q} \\
= \frac{(y_P - y_Q)x - x_P y_P + x_P y_Q + x_P y_P - x_Q y_P}{x_P - x_Q} \\
= \frac{(y_P - y_Q)x + x_P y_Q - x_Q y_P + (x_Q y_Q - x_Q y_Q)}{x_P - x_Q} \\
= \frac{(y_P - y_Q)x - (y_P - y_Q)x_Q + (x_P - x_Q)y_Q}{x_P - x_Q} \\
= \frac{y_P - y_Q}{x_P - x_Q}(x - x_Q) + y_Q \tag{21}
\end{array}
$$

表达式 (20) 和 (21) 除了交换 $P$ 和 $Q$ 之外是相等的。因此，对所有 $P, Q \in E$ 我们有 $P \bullet Q = Q \bullet P$。

最后一步是证明 • 满足结合律：$(a \bullet b) \bullet c = a \bullet (b \bullet c)$。不幸的是，证明结合律比其他性质复杂得多：该性质的证明涉及一些高等数学，或使用计算机代数软件包 [17, 18]。因此我们将跳过该性质的证明。

作为替代，为了检查我们的结果，我们可以在显式公式数据库 (EFD) [5] 中查找 Montgomery 曲线，该数据库列出了群律如下：

```
name     Montgomery curves
parameter a
parameter b
coordinate x
coordinate y
satisfying b*y^2 = x^3 + a*x^2 + x
addition   x = b*(y2-y1)^2/(x2-x1)^2 - a - x1 - x2
addition   y = (2*x1+x2+a)*(y2-y1)/(x2-x1) - b*(y2-y1)^3/(x2-x1)^3 - y1
doubling   x = b*(3*x1^2+2*a*x1+1)^2/(2*b*y1)^2 - a - x1 - x1
doubling   y = (2*x1+x1+a)*(3*x1^2+2*a*x1+1)/(2*b*y1) - b*(3*x1^2+2*a*x1+1)^3/(2*b*y1)^3 - y1
```

该数据库使用略更一般的形式 $By^2 = x^3 + Ax^2 + x$，带有一个额外参数 $B$。我们可以看到，当 $B = 1$ 时，数据库中的公式等于我们的公式 (18) 和 (19)。EFD 中的公式使用 SageMath 检查过；我们在此不重复这些检查。

**关于记号的说明。** 在椭圆曲线文献中，群运算 • 传统上写作 +，$P$ 的逆元写作 $-P$，使用群运算组合两个不同点 $P$ 和 $Q$ 的 $P + Q$ 称为点加法，将点 $P$ 与自身组合 $P + P = 2P$ 称为点倍增。另一方面，在密码学协议文献中，群运算传统上写作乘法 ·，$P$ 的逆元写作 $P^{-1}$，将群元素与自身组合写作 $P \cdot P = P^2$。在本文中，我们使用 • 作为椭圆曲线群元素 $E$ 上的群运算，以避免与单个域元素（即模 $p$ 的整数）的加法和乘法混淆，后者在上面的公式中使用。

## 4.3 椭圆曲线 Diffie-Hellman

现在我们已经构造了一个群，我们可以将这个群用于密码学协议。特别地，我们可以使用 Curve25519 群来实现 X25519 Diffie-Hellman 密钥交换函数。

对于群元素 $P \in E$ 和非负整数 $k$，我们如前定义群运算符对 $P$ 的重复应用：

$$
P^k = \underbrace{P \bullet P \bullet \cdots \bullet P}_{k \text{ 次}} \tag{22}
$$

我们在第 4.4 节中展示如何高效计算 $P^k$，即使 $k$ 很大。这个运算称为标量乘法（其中"标量"指的是 $k$ 是整数，而不是群元素）。

我们可以将序列 $P, P^2, P^3, \ldots$ 可视化为反复画一条通过点 $P$ 和 $P^i$ 的直线（如图 1 所示），找到该线与曲线的第三交点，并关于 $x$ 轴镜像该点以获得点 $P^{i+1}$。直觉上的效果是一系列点以复杂且难以预测的模式在曲线上"跳跃"，如图 3 所示。这种复杂的模式正是使该群适合密码学的原因。特别地，人们相信判定性 Diffie-Hellman 假设（见第 2.4 节）在这个群中成立。

该群中离散对数的最优已知算法，如 Pollard 的 rho 算法 [28]，本质上归结为尝试大量 $k$ 值直到找到与输入 $P^k$ 匹配的结果。该算法大约需要 $O(\sqrt{k})$ 时间来找到 $k$。因此，如果我们选择 $k$ 为 $n$ 位长，所需时间为 $O(2^{n/2})$。X25519 选择 $n = 251$（小于 255 是因为四个比特被设置为常数值），使得计算离散对数的难度与破解 128 位对称密码相当。

代码清单 4 展示了如何实现 Diffie-Hellman。scalarmult(out, scalar, point) 函数接受三个参数：point 是输入群元素 $P$，scalar 是标量指数，out 是指向将写入输出 $P^{\text{scalar}}$ 的内存的指针。point、scalar 和 out 都是 255 位数，编码为 32 字节的数组。实际上，point 和 out 不是完整的群元素，而只是曲线上点的 $x$ 坐标；我们将在第 4.4 节看到为什么可以省略 $y$ 坐标。我们将在代码清单 5 中看到 scalarmult 的实现。

scalarmult_base(out, scalar) 使用固定的群元素 \_9 执行标量乘法，这是一个 $x$ 坐标等于 9 的曲线点。选择这个点是因为它的阶是一个大素数，如第 2.7 节所述。代码清单 3 展示了如何使用 SageMath 检查该点的阶。结果同样返回到 out 中。

generate_keypair(pk, sk) 生成一个新的密钥对，其中私钥写入 sk，公钥写入 pk。私钥由从安全的均匀随机数源抽取的 32 字节（256 位）组成。公钥通过将 sk 与固定基点进行标量乘法获得。

x25519(out, pk, sk) 由消息的发送方和接收方共同调用。如果由发送方调用，pk 是接收方的公钥，sk 是发送方生成的随机整数（第 2.3 节示例中 Alice 的 $k$）。如果由接收方调用，pk 是随消息发送的群元素（第 2.3 节示例中的 $g^k$），sk 是接收方的私钥。在任一情况下，计算群元素与秘密整数的标量积产生共享秘密，写入 out。共享秘密随后可用于初始化对称密码来加密实际消息，但这超出了 X25519 的范围，因此我们在本文中不再进一步讨论。

![图 3](https://cdn-mineru.openxlab.org.cn/result/2026-06-03/4843056d-5136-4620-80c6-28bca21ee9d8/05691b386b7e69746e4aac4d1b286049aa181085c806b9090e0d1e44b345208b.jpg)

图 3. 使用群运算符反复将曲线点 $P$ 与自身组合。每次运算的应用可以可视化为画一条通过 $P$ 和 $P^i$ 的直线，找到该线与曲线的第三交点，并关于 $x$ 轴镜像该点以获得 $P^{i+1}$。结果是一系列以伪随机方式"跳跃"的点。

```c
typedef unsigned char u8;
typedef unsigned long long u64;
extern void randombytes(u8 *, u64);
static const u8 _9[32] = {9};

void scalarmult_base(u8 *out, const u8 *scalar)
{
    scalarmult(out, scalar, _9);
}

void generate_keypair(u8 *pk, u8 *sk)
{
    randombytes(sk, 32);
    scalarmult_base(pk, sk);
}

void x25519(u8 *out, const u8 *pk, const u8 *sk)
{
    scalarmult(out, sk, pk);
}
```

代码清单 4. 使用 scalarmult 函数实现 Diffie-Hellman。

## 4.4 Montgomery 阶梯

Montgomery 阶梯 [6, 14, 27] 是 X25519 用来高效执行标量乘法的算法。函数 $L(P, i)$ 接受一个群元素 $P \neq \infty$ 和一个非负整数 $i$，返回一对群元素 $(P^i, P^{i+1})$，使用群运算 • 计算：

$$
L(P, 0) = (\infty, P) \tag{23}
$$

$$
L(P, 2i) = (P_i \bullet P_i, P_i \bullet P_{i+1}) \quad \text{其中 } L(P, i) = (P_i, P_{i+1}) \text{ 且 } i > 0
$$

$$
L(P, 2i + 1) = (P_i \bullet P_{i+1}, P_{i+1} \bullet P_{i+1}) \quad \text{其中 } L(P, i) = (P_i, P_{i+1}) \text{ 且 } i \geq 0
$$

这里 $P^0 = \infty$，因为 ∞ 是群的单位元。第二个参数在每次递归调用时减半（向下取整，即右移一位），结果是计算 $L(P, i)$ 需要 $\lfloor \log_2 i \rfloor + 1$ 次递归调用（即 $i$ 的每个比特一次调用）。在实践中，我们可以用循环代替递归，在循环的每次迭代中检查 $i$ 的一个比特，从最高有效位开始。（第 28 页的代码清单 5 展示了我们对 Montgomery 阶梯的实现；第 19 到 43 行包含遍历整数比特的主循环。）在每次循环迭代中，我们执行两次群运算：

- 如果比特为零（即 $2i$ 的情况），我们将 $P_i$ 与自身组合产生 $P^i \bullet P^i = P^{2i}$，并将 $P_i$ 与 $P_{i+1}$ 组合产生 $P^i \bullet P^{i+1} = P^{2i+1}$。

- 如果比特为一（即 $2i + 1$ 的情况），我们将 $P_i$ 与 $P_{i+1}$ 组合产生 $P^i \bullet P^{i+1} = P^{2i+1}$，并将 $P_{i+1}$ 与自身组合产生 $P^{i+1} \bullet P^{i+1} = P^{2i+2}$。

我们返回两个群元素而非一个的原因是，正如我们将很快看到的，给定 $P$、$P^i$ 和 $P^{i+1}$，计算 $P^i \bullet P^{i+1} = P^{2i+1}$ 有一个特别高效的公式，比直接使用 (18)、(19) 中的群律更快。

在 X25519 中，我们需要执行这个循环 255 次（标量乘法函数中的整数是 256 位，但我们设置最高位始终为零，因此只需 255 次迭代）。由于这是算法中最耗时的部分，我们现在研究如何尽可能快地执行这些群运算。

**使用射影坐标。** 首先注意方程 (18) 和 (19) 包含分数。如果我们在循环的每次迭代中都必须执行除法，乘法逆元运算将成为算法中最慢的部分。为了节省时间，我们可以将坐标表示为分数：$x = X/Z$ 和 $y = Y/Z$，其中 $X$、$Y$ 和 $Z$ 是整数。（这些称为射影坐标，而我们迄今使用的 $(x, y)$ 坐标称为仿射坐标。）在每次循环迭代中，我们分别计算坐标的分子和分母，不进行除法，只在循环结束后执行一次实际的除法。

**点倍增的射影公式。** 让我们首先推导将群元素与自身组合的优化公式：$P_{2i} = (x_{2i}, y_{2i}) = P_i \bullet P_i = (x, y) \bullet (x, y)$。使用 (18) 中的表达式：

$$
\begin{array}{l}
x_{2i} = \frac{(3x^2 + 2Ax + 1)^2}{4y^2} - A - 2x \\
= \frac{(3x^2 + 2Ax + 1)^2}{4(x^3 + Ax^2 + x)} - A - 2x \quad \text{因为 } (x, y) \text{ 在曲线 } y^2 = x^3 + Ax^2 + x \text{ 上} \\
= \frac{(3x^2 + 2Ax + 1)^2 - 4(x^3 + Ax^2 + x)(2x + A)}{4(x^3 + Ax^2 + x)} \\
= \frac{9x^4 + 12Ax^3 + (4A^2 + 6)x^2 + 4Ax + 1 - 4(2x^4 + 3Ax^3 + (A^2 + 2)x^2 + Ax)}{4(x^3 + Ax^2 + x)} \\
= \frac{x^4 - 2x^2 + 1}{4(x^3 + Ax^2 + x)} = \frac{\frac{X^4}{Z^4} - \frac{2X^2}{Z^2} + 1}{4\left(\frac{X^3}{Z^3} + \frac{AX^2}{Z^2} + \frac{X}{Z}\right)} \quad \text{使用射影坐标 } x = \frac{X}{Z} \tag{24} \\
= \frac{\frac{1}{Z^4}(X^4 - 2X^2 Z^2 + Z^4)}{\frac{4}{Z^3}(X^3 + AX^2 Z + XZ^2)} = \frac{X^4 - 2X^2 Z^2 + Z^4}{4XZ(X^2 + AXZ + Z^2)} = \frac{(X^2 - Z^2)^2}{4XZ(X^2 + AXZ + Z^2)}
\end{array}
$$

因此，我们得到以下点倍增的射影公式：

$$
\left(\frac{X_i}{Z_i}, \frac{Y_i}{Z_i}\right) \bullet \left(\frac{X_i}{Z_i}, \frac{Y_i}{Z_i}\right) = \left(\frac{X_{2i}}{Z_{2i}}, \frac{Y_{2i}}{Z_{2i}}\right) \quad \text{其中} \quad \begin{array}{l} X_{2i} = (X_i^2 - Z_i^2)^2 \\ Z_{2i} = 4X_i Z_i (X_i^2 + AX_i Z_i + Z_i^2) \end{array} \tag{25}
$$

注意 $X_{2i}$ 和 $Z_{2i}$ 的公式完全没有使用 $y$ 坐标。这意味着我们可以从一开始就避免计算任何 $y$ 坐标，甚至不需要推导 $Y_{2i}$ 的表达式。接下来，我们将推导同样避免使用任何 $y$ 坐标的点加法公式。

**点加法的射影公式。** 我们现在推导群运算 $P_{2i+1} = (x_{2i+1}, y_{2i+1}) = P_i \bullet P_{i+1} = (x_1, y_1) \bullet (x_2, y_2)$ 的公式。我们假设 $P_i \neq P_{i+1}$（因为我们知道 $P_{i+1} = P^{i+1} = P^i \bullet P = P_i \bullet P$ 且我们假设 $P \neq \infty$），我们还假设 $P_i^{-1} \neq P_{i+1}$（在这种情况下，$P_{2i+1} = \infty$）。因此我们可以假设 $x_1 \neq x_2$。以方程 (18) 为出发点：

$$
\begin{array}{l}
x_{2i+1} = \left(\frac{y_2 - y_1}{x_2 - x_1}\right)^2 - A - x_1 - x_2 \\
= \frac{y_2^2 - 2y_1 y_2 + y_1^2 - (A + x_1 + x_2)(x_2 - x_1)^2}{(x_2 - x_1)^2} \\
= \frac{x_2^3 + Ax_2^2 + x_2 + x_1^3 + Ax_1^2 + x_1 - 2y_1 y_2}{(x_2 - x_1)^2} \\
- \frac{Ax_2^2 - 2Ax_1 x_2 + Ax_1^2 + x_1 x_2^2 - 2x_1^2 x_2 + x_1^3 + x_2^3 - 2x_1 x_2^2 + x_1^2 x_2}{(x_2 - x_1)^2} \\
= \frac{x_1 + x_2 + x_1^2 x_2 + x_1 x_2^2 + 2Ax_1 x_2 - 2y_1 y_2}{(x_2 - x_1)^2} \\
= \frac{(x_1 + x_2)(1 + x_1 x_2) + 2Ax_1 x_2 - 2y_1 y_2}{(x_2 - x_1)^2} \tag{26}
\end{array}
$$

接下来，我们利用 $P_{i+1} = P_i \bullet P$ 这一事实。因为群的每个元素都有逆元，我们有 $P_i^{-1} \bullet P_{i+1} = P_i^{-1} \bullet P_i \bullet P = P$。设 $P = (x_P, y_P)$，$P_i = (x_1, y_1)$，$P_{i+1} = (x_2, y_2)$。因为 $P_i^{-1} = (x_1, -y_1)$，我们有

$$
P = (x_P, y_P) = P_i^{-1} \bullet P_{i+1} = (x_1, -y_1) \bullet (x_2, y_2) \quad \Longrightarrow \quad x_P = \frac{(x_1 + x_2)(1 + x_1 x_2) + 2Ax_1 x_2 + 2y_1 y_2}{(x_2 - x_1)^2}
$$

即 $x_P$ 等于将 $y_1$ 取反后的表达式 (26)。为了从 (26) 中清除剩余的 $y_1$ 和 $y_2$ 出现（不引入平方根），我们将 $x_{2i+1}$ 和 $x_P$ 相乘（推导来自 [6]）：

$$
\begin{array}{l}
x_P x_{2i+1} (x_2 - x_1)^4 = ((x_1 + x_2)(1 + x_1 x_2) + 2Ax_1 x_2 + 2y_1 y_2)((x_1 + x_2)(1 + x_1 x_2) + 2Ax_1 x_2 - 2y_1 y_2) \\
= ((x_1 + x_2)(1 + x_1 x_2) + 2Ax_1 x_2)^2 - (2y_1 y_2)^2 \\
= ((x_1 + x_2)(1 + x_1 x_2) + 2Ax_1 x_2)^2 - 4(x_1^3 + Ax_1^2 + x_1)(x_2^3 + Ax_2^2 + x_2) \\
= (x_1 + x_2)^2 (1 + x_1 x_2)^2 + 4Ax_1 x_2 (x_1 + x_2)(1 + x_1 x_2) + 4A^2 x_1^2 x_2^2 \\
- 4(x_1^3 + x_1)(x_2^3 + x_2) - 4Ax_1^2 (x_2^3 + x_2) - 4Ax_2^2 (x_1^3 + x_1) - 4A^2 x_1^2 x_2^2 \\
= (x_1 + x_2)^2 (1 + x_1 x_2)^2 + 4Ax_1 x_2 (x_1 + x_2 + x_1^2 x_2 + x_1 x_2^2) \\
- 4(x_1^3 + x_1)(x_2^3 + x_2) - 4Ax_1 x_2 (x_1 x_2^2 + x_1 + x_1^2 x_2 + x_2) \\
= (x_1 + x_2)^2 (1 + x_1 x_2)^2 - 4(x_1^3 + x_1)(x_2^3 + x_2) \\
= (x_1^2 + 2x_1 x_2 + x_2^2)(1 + 2x_1 x_2 + x_1^2 x_2^2) - 4(x_1^3 x_2^3 + x_1 x_2^3 + x_1^3 x_2 + x_1 x_2) \\
= x_1^2 + 2x_1 x_2 + x_2^2 + 2x_1^3 x_2 + 4x_1^2 x_2^2 + 2x_1 x_2^3 + x_1^4 x_2^2 + 2x_1^3 x_2^3 + x_1^2 x_2^4 \\
- 4x_1^3 x_2^3 - 4x_1 x_2^3 - 4x_1^3 x_2 - 4x_1 x_2 \\
= x_1^2 - 2x_1 x_2 + x_2^2 - 2x_1^3 x_2 + 4x_1^2 x_2^2 - 2x_1 x_2^3 + x_1^4 x_2^2 - 2x_1^3 x_2^3 + x_1^2 x_2^4 \\
= (x_2 - x_1)^2 - 2x_1 x_2 (x_2 - x_1)^2 + x_1^2 x_2^2 (x_2 - x_1)^2 \\
= (x_2 - x_1)^2 (x_1 x_2 - 1)^2
\end{array}
$$

因此，在假设 $x_P \neq 0$ 下，我们得到

$$
\begin{array}{l}
x_{2i+1} = \frac{(x_1 x_2 - 1)^2}{x_P (x_2 - x_1)^2} = \frac{\left(\frac{X_1 X_2}{Z_1 Z_2} - 1\right)^2}{\frac{X_P}{Z_P} \left(\frac{X_2}{Z_2} - \frac{X_1}{Z_1}\right)^2} \quad \text{其中} \quad x_P = \frac{X_P}{Z_P}, x_1 = \frac{X_1}{Z_1}, x_2 = \frac{X_2}{Z_2} \\
= \frac{Z_P \left(\frac{X_1^2 X_2^2}{Z_1^2 Z_2^2} - \frac{2X_1 X_2}{Z_1 Z_2} + 1\right)}{X_P \left(\frac{X_2^2}{Z_2^2} - \frac{2X_1 X_2}{Z_1 Z_2} + \frac{X_1^2}{Z_1^2}\right)} = \frac{\frac{Z_P}{Z_1^2 Z_2^2}(X_1^2 X_2^2 - 2X_1 X_2 Z_1 Z_2 + Z_1^2 Z_2^2)}{\frac{X_P}{Z_1^2 Z_2^2}(X_2^2 Z_1^2 - 2X_1 X_2 Z_1 Z_2 + X_1^2 Z_2^2)} \\
= \frac{Z_P (X_1 X_2 - Z_1 Z_2)^2}{X_P (X_1 Z_2 - X_2 Z_1)^2} \tag{27}
\end{array}
$$

在 X25519 中，标量乘法的基点 $x_P = X_P/Z_P$ 以仿射坐标给出（即不是分数形式），且 $P \neq \infty$，因此我们可以假设 $Z_P = 1$ 和 $X_P = x_P$。

因此，我们可以使用 (25) 和 (27) 的公式计算 Montgomery 阶梯的一步：

$$
X_{2i} = (X_i^2 - Z_i^2)^2 \quad X_{2i+1} = (X_i X_{i+1} - Z_i Z_{i+1})^2 \tag{28}
$$

$$
Z_{2i} = 4X_i Z_i (X_i^2 + AX_i Z_i + Z_i^2) \qquad Z_{2i+1} = x_P (X_i Z_{i+1} - X_{i+1} Z_i)^2
$$

这些公式从不使用 $y$ 坐标，允许我们仅在 $x$ 坐标上操作。它们接受标量的一个比特、上一步的输出 $(X_i, Z_i, X_{i+1}, Z_{i+1})$ 以及基点 $x$ 坐标 $x_P$ 作为输入。如果当前比特为零，它们产生 $(X_{2i}, Z_{2i}, X_{2i+1}, Z_{2i+1})$ 作为输出，如 (23) 所示。如果当前比特为一，它们产生 $(X_{2i+1}, Z_{2i+1}, X_{2i+2}, Z_{2i+2})$ 作为输出，其中 $X_{2i+2}$ 和 $Z_{2i+2}$ 通过对 $(X_{i+1}, Z_{i+1})$（而非 $(X_i, Z_i)$）应用倍增公式来计算。

这些公式的一个理想性质是，阶梯的每一步执行完全相同的算术运算，无论输入坐标和标量的比特如何，使算法成为常数时间的。

## 4.5 处理无穷远点

在上述公式的推导中，我们迄今只考虑了曲线方程的解的点，而忽略了无穷远点 ∞。现在是我们处理这个问题的时候了。

无穷远点不能用仿射坐标 $(x, y)$ 表示，对于任何有限的 $x, y$ 都不行。然而，使用射影坐标的一个便利特性是，我们可以将无穷远点表示为分母为零的分数：我们将 ∞ 的 $x$ 坐标定义为 $\frac{X}{0}$，即 $Z = 0$。我们不允许 $X$ 和 $Z$ 同时为零。我们可以忽略 $y$ 坐标，因为我们的公式不使用它。

幸运的是，我们的公式 (28) 已经正确处理了无穷远点。我们通过证明如果任何输入为 ∞ 时它们产生所需结果来演示这一点。此外，我们证明只要每个输入是有效的，即 $(X, Z) \neq (0, 0)$，则输出也将不同于 $(0, 0)$。以下假设 $x_P \neq 0$。

- 设 $Z_i = 0$ 且 $X_i \neq 0$，所以 $P_i = \infty$。则 $Z_{2i} = 0$ 且 $X_{2i} = X_i^4 \neq 0$，所以 $P_{2i} = P_i \bullet P_i = \infty \bullet \infty = \infty$，符合 (15) 的要求。

- 设 $Z_i \neq 0$。根据 $X_i^3 + AX_i^2 Z_i + X_i Z_i^2$ 的值，我们考虑两种情况：

  (1) 假设 $X_i^3 + AX_i^2 Z_i + X_i Z_i^2 = 0$；因此 $Z_{2i} = 0$。为了证明 $X_{2i} \neq 0$，假设相反 $X_{2i} = 0$；则 $X_i^2 = Z_i^2$，所以 $Z_i = \pm X_i$。代入 $X_i^3 + AX_i^2 Z_i + X_i Z_i^2 = 0$ 得 $X_i^3 \pm AX_i^3 + X_i^3 = 0$，所以 $AX_i^3 = \pm 2X_i^3$。由于 $Z_i \neq 0$ 我们有 $X_i \neq 0$，因此 $A = \pm 2$。然而，这与第 4 节开头陈述的假设 $A^2 \neq 4$ 矛盾。因此我们有 $(X_{2i}, Z_{2i}) \neq (0, 0)$，符合要求。

  (2) 假设 $X_i^3 + AX_i^2 Z_i + X_i Z_i^2 \neq 0$，这意味着 $X_i \neq 0$。由于 $Z_i \neq 0$ 我们有 $Z_{2i} \neq 0$，所以我们有 $(X_{2i}, Z_{2i}) \neq (0, 0)$，符合要求。

- 设 $Z_i \neq 0, Z_{i+1} \neq 0$，且 $P_i$ 与 $P_{i+1}$ 具有相同的 $x$ 坐标，即 $X_i/Z_i = X_{i+1}/Z_{i+1}$。这意味着两种情况之一：要么 $P_i = P_{i+1}$，要么 $P_i^{-1} = P_{i+1}$。前者被我们 $P \neq \infty$ 的假设排除，所以我们有 $P_i^{-1} = P_{i+1}$ 且要求 $P_i \bullet P_{i+1} = \infty$，如 (16) 所述。$X_i/Z_i = X_{i+1}/Z_{i+1}$ 意味着 $X_i Z_{i+1} = X_{i+1} Z_i$，所以 $Z_{2i+1} = 0$，符合要求。

  接下来，我们需要证明 $X_{2i+1} \neq 0$。假设相反 $X_{2i+1} = 0$，意味着 $X_i X_{i+1} - Z_i Z_{i+1} = 0$。结合我们之前证明的事实 $X_i Z_{i+1} = X_{i+1} Z_i$，我们有 $X_i X_{i+1} - Z_i Z_{i+1} - X_i Z_{i+1} + X_{i+1} Z_i = (X_i + Z_i)(X_{i+1} - Z_{i+1}) = 0$。我们现在有两种情况：

  (1) 如果 $X_i + Z_i = 0$，则 $X_i = -Z_i$，所以 $X_i/Z_i = -1$。使用假设 $X_i/Z_i = X_{i+1}/Z_{i+1}$，我们有 $X_{i+1}/Z_{i+1} = x_{i+1} = -1$，所以 $P_{i+1}$ 的仿射 $x$ 坐标等于 −1。

  (2) 如果 $X_i + Z_i \neq 0$，则 $X_{i+1} = Z_{i+1}$，所以 $X_{i+1}/Z_{i+1} = x_{i+1} = 1$，所以 $P_{i+1}$ 的仿射 $x$ 坐标等于 1。

  在两种情况下，我们使用点倍增表达式 (24) 计算 $P_{i+1} \bullet P_{i+1}$ 的 $x$ 坐标，即 $(x_{i+1}^4 - 2x_{i+1}^2 + 1)/(4x_{i+1}^3 + 4Ax_{i+1}^2 + 4x_{i+1}) = 0$（当 $x_{i+1} = \pm 1$ 时）。注意 $P_{i+1} = P_i \bullet P$，所以 $P = P_i^{-1} \bullet P_{i+1} = P_{i+1} \bullet P_{i+1}$，因为我们之前观察到 $P_i^{-1} = P_{i+1}$。因此，$P$ 的 $x$ 坐标等于零，这与我们之前 $x_P \neq 0$ 的假设矛盾。因此我们有 $X_{2i+1} \neq 0$，符合要求。

- 设 $Z_i \neq 0, Z_{i+1} \neq 0$，且 $P_i$ 与 $P_{i+1}$ 具有不同的 $x$ 坐标，即 $X_i/Z_i \neq X_{i+1}/Z_{i+1}$。则 $X_i Z_{i+1} \neq X_{i+1} Z_i$，所以 $Z_{2i+1} \neq 0$，所以 $P_{2i+1} \neq \infty$，符合要求。

- 设 $Z_i = 0$ 且 $X_i \neq 0$，所以 $P_i = \infty$。由于 $P_{i+1} = P_i \bullet P = \infty \bullet P = P$ 且 $P \neq \infty$，我们有 $Z_{i+1} \neq 0$ 且 $X_{i+1}/Z_{i+1} = x_P$，所以 $X_{i+1} = x_P Z_{i+1}$。因此，$X_{2i+1} = (X_i X_{i+1})^2 = x_P^2 X_i^2 Z_{i+1}^2$ 且 $Z_{2i+1} = x_P (X_i Z_{i+1})^2 \neq 0$。因此，$X_{2i+1}/Z_{2i+1} = x_P^2 X_i^2 Z_{i+1}^2 / x_P X_i^2 Z_{i+1}^2 = x_P$，所以 $P_{2i+1} = P_i \bullet P_{i+1} = \infty \bullet P = P$，符合 (15) 的要求。

- 设 $Z_{i+1} = 0$ 且 $X_{i+1} \neq 0$，所以 $P_{i+1} = \infty$。由于 $P_{i+1} = P_i \bullet P$ 且 $P \neq \infty$，我们有 $P_i = P^{-1}$ 且 $Z_i \neq 0$。$P^{-1}$ 的 $x$ 坐标与 $P$ 的 $x$ 坐标相同，即 $x_P$，所以 $X_i/Z_i = x_P$；所以 $X_i = x_P Z_i$。因此，类似上一种情况，我们有 $X_{2i+1} = (X_i X_{i+1})^2 = x_P^2 X_{i+1}^2 Z_i^2$ 且 $Z_{2i+1} = x_P (-X_{i+1} Z_i)^2 \neq 0$。因此，$X_{2i+1}/Z_{2i+1} = x_P^2 X_{i+1}^2 Z_i^2 / x_P X_{i+1}^2 Z_i^2 = x_P$，这与 $P_{2i+1} = P_i \bullet P_{i+1} = P^{-1} \bullet \infty = P^{-1}$ 一致，符合 (15) 的要求。

以上要点涵盖了所有可能的情况，证明了公式 (28) 正确处理了所有群元素，包括无穷远点，无需对边界情况进行任何特殊处理。这是个好消息，因为没有边界情况简化了 Montgomery 阶梯的实现。此外，如果阶梯的每一步都执行完全相同的算术运算，独立于其输入的值，则更容易使算法成为常数时间的。

## 4.6 优化 Montgomery 阶梯步骤

公式 (28) 简洁优美，但如果我们将其与代码清单 5 中的 Montgomery 阶梯实现进行比较，两者看起来相当不同。原因是代码基于进一步优化的公式。

我们已经通过转向射影坐标避免了在 Montgomery 阶梯步骤中使用除法。为了进一步提高性能，我们的目标是尽可能减少有限域乘法的数量，因为它们通常是除法之后最昂贵的运算。我们忽略加法和减法，因为相比之下它们很便宜。

如果我们将公式 (28) 分解为每个方程一次乘法，尽可能重用公共子表达式，我们看到阶梯的每一步需要 14 次乘法：

$$
\begin{array}{l}
v_1 = X_i^2 \qquad v_5 = Av_3 \qquad v_6 = X_i X_{i+1} \qquad v_{10} = (v_8 - v_9)^2 \\
v_2 = Z_i^2 \qquad X_{2i} = (v_1 - v_2)^2 \qquad v_7 = Z_i Z_{i+1} \qquad X_{2i+1} = (v_6 - v_7)^2 \\
v_3 = X_i Z_i \qquad Z_{2i} = v_4 (v_1 + v_5 + v_2) \qquad v_8 = X_i Z_{i+1} \qquad Z_{2i+1} = x_P v_{10} \\
v_4 = 4v_3 \qquad v_9 = X_{i+1} Z_i
\end{array}
$$

一些作者将乘以常数（$v_4$ 和 $v_5$ 的情况下的 4 或 $A$）和平方分别计算。然而，我们的实现在所有情况下使用相同的乘法函数，因此为简单起见，我们平等计算所有类型的乘法。

与上述 14 次乘法相比，代码清单 5 中的实现每步只使用 10 次乘法。在本节中，我们聚焦于如何从公式 (28) 推导出这个算法。代码清单 5 第 19 到 43 行的主阶梯循环执行以下操作：

- `bit = (clamped[i >> 3] >> (i & 7)) & 1` 将 bit 设置为小端字节数组 clamped 的第 i 个比特（之前被设置为参数 scalar 的副本，带有一些稍后解释的调整）。

- swap25519(a, b, bit) 检查 bit，它要么是 0 要么是 1。如果 bit == 0，该函数什么都不做。如果 bit == 1，该函数交换两个变量 a 和 b 中的值。它以常数时间完成此操作，因此"什么都不做"的情况与交换情况花费相同的执行时间。

- fadd、fsub 和 fmul 执行域元素的加法、减法和乘法，如第 3 节所定义。

循环的每次迭代接受变量 a、b、c、d、x 和 clamped 中的值作为输入，以及常数 `_121665 = {0xDB41, 1}`（包含数字 0x1DB41 = 121665，使用 field_elem 表示拆分为 16 位块）。作为输出，它向变量 a、b、c 和 d 写入新值。此外，它使用 e 和 f 作为临时变量。在代码清单 5 的代码中，变量被重用。为了更好的可读性，我们在以下操作分解中为每个变量赋值给出新名称。Montgomery 阶梯循环中的 18 个算术运算（10 次乘法/平方和 8 次加法/减法）计算以下表达式：

```c
typedef long long i64;
typedef i64 field_elem[16];
static const field_elem _121665 = {0xDB41, 1};

void scalarmult(u8 *out, const u8 *scalar, const u8 *point)
{
    u8 clamped[32];
    i64 bit, i;
    field_elem a, b, c, d, e, f, x;
    for (i = 0; i < 32; ++i) clamped[i] = scalar[i];
    clamped[0] &= 0xf8;
    clamped[31] = (clamped[31] & 0x7f) | 0x40;
    unpack25519(x, point);
    for (i = 0; i < 16; ++i) {
    b[i] = x[i];
    d[i] = a[i] = c[i] = 0;
    }
    a[0] = d[0] = 1;
    for (i = 254; i >= 0; --i) {
    bit = (clamped[i >> 3] >> (i & 7)) & 1;
    swap25519(a, b, bit);
    swap25519(c, d, bit);
    fadd(e, a, c);
    fsub(a, a, c);
    fadd(c, b, d);
    fsub(b, b, d);
    fmul(d, e, e);
    fmul(f, a, a);
    fmul(a, c, a);
    fmul(c, b, e);
    fadd(e, a, c);
    fsub(a, a, c);
    fmul(b, a, a);
    fsub(c, d, f);
    fmul(a, c, _121665);
    fadd(a, a, d);
    fmul(c, c, a);
    fmul(a, d, f);
    fmul(d, b, x);
    fmul(b, e, e);
    swap25519(a, b, bit);
    swap25519(c, d, bit);
    }
    finverse(c, c);
    fmul(a, a, c);
    pack25519(out, a);
}
```

代码清单 5. 用于标量乘法的 Montgomery 阶梯。

`fadd(e, a, c);` $\qquad v_1 = a + c$

`fsub(a, a, c);` $\qquad v_2 = a - c$

`fadd(c, b, d);` $\qquad v_3 = b + d$

`fsub(b, b, d);` $\qquad v_4 = b - d$

`fmul(d, e, e);` $\qquad v_5 = v_1^2 = (a + c)^2$

`fmul(f, a, a);` $\qquad v_6 = v_2^2 = (a - c)^2$

`fmul(a, c, a);` $\qquad v_7 = v_3 \cdot v_2 = (b + d)(a - c) = ab - bc + ad - cd$

`fmul(c, b, e);` $\qquad v_8 = v_4 \cdot v_1 = (b - d)(a + c) = ab + bc - ad - cd$

`fadd(e, a, c);` $\qquad v_9 = v_7 + v_8 = 2(ab - cd)$

`fsub(a, a, c);` $\qquad v_{10} = v_7 - v_8 = 2(ad - bc)$

`fmul(b, a, a);` $\qquad v_{11} = v_{10}^2 = 4(ad - bc)^2$

`fsub(c, d, f);` $\qquad v_{12} = v_5 - v_6 = (a + c)^2 - (a - c)^2 = 4ac$

`fmul(a, c, _121665);` $\qquad v_{13} = 121665 \cdot v_{12} = 486660\,ac = (A - 2)ac$

`fadd(a, a, d);` $\qquad v_{14} = v_{13} + v_5 = (A - 2)ac + a^2 + 2ac + c^2 = a^2 + Aac + c^2$

`fmul(c, c, a);` $\qquad v_{15} = v_{12} \cdot v_{14} = 4ac(a^2 + Aac + c^2)$

`fmul(a, d, f);` $\qquad v_{16} = v_5 \cdot v_6 = (a + c)^2 (a - c)^2 = (a^2 - c^2)^2$

`fmul(d, b, x);` $\qquad v_{17} = v_{11} \cdot x = 4x(ad - bc)^2$

`fmul(b, e, e);` $\qquad v_{18} = v_9^2 = 4(ab - cd)^2$

设循环迭代开始时 $a = X_i, b = X_{i+1}, c = Z_i, d = Z_{i+1}$，$x = x_P$。进一步设 bit 为 0，所以 swap25519 操作没有效果。则上述表达式与方程 (28) 匹配，其中 $A = 486662$，$v_{16} = X_{2i}, v_{18} = 4X_{2i+1}, v_{15} = Z_{2i}$，$v_{17} = 4Z_{2i+1}$。值 $(v_{16}, v_{18}, v_{15}, v_{17})$ 分别写入变量 a、b、c 和 d，形成下一次迭代的输入。

如果 bit 为 1，a 和 b 中的值在计算这些表达式之前和之后被交换，同样 c 和 d 中的值也被交换。因此，计算的输入是 $a = X_{i+1}, b = X_i, c = Z_{i+1}$，$d = Z_i$，输出是 $v_{16} = X_{2i+2}, v_{18} = 4X_{2i+1}, v_{15} = Z_{2i+2}$，$v_{17} = 4Z_{2i+1}$。在最终交换之后，变量 a、b、c 和 d 包含值 $(v_{18}, v_{16}, v_{17}, v_{15})$。

因此，每次循环迭代将输入元组 $(X_i, Z_i, X_{i+1}, Z_{i+1})$ 映射到输出元组 $(X_{2i}, Z_{2i}, 4X_{2i+1}, 4Z_{2i+1})$ 或 $(4X_{2i+1}, 4Z_{2i+1}, X_{2i+2}, Z_{2i+2})$，取决于 bit 的值。这完全匹配 Montgomery 阶梯步骤 (23)，除了 $X_{2i+1}$ 和 $Z_{2i+1}$ 中额外的因子 4。然而，由于这两个变量只是分数 $x_{2i+1} = X_{2i+1}/Z_{2i+1}$ 的扩展表示，这两个因子 4 相互抵消，对最终结果没有影响。

## 4.7 钳位处理

我们现在转向 scalarmult 函数的前几行。

标量参数 scalar 被假定为 32 字节的均匀分布随机数组。首先，scalar 被复制到 clamped，然后 clamped 的五个比特被设置为常数值。`clamped[0] &= 0xf8` 将最低三位设置为 0。`clamped[31] = (clamped[31] & 0x7f) | 0x40` 将最高位设置为 0，次高位设置为 1。这个过程称为钳位处理 [25]。

将最低三位设置为 0 确保 clamped 是 8 的倍数。同时将最高位设置为 0，clamped 变为 $hk$ 形式的数，其中 $h = 8$，$k$ 是从 $[0, 2^{252} - 1]$ 范围内均匀分布的随机数。如第 2.7 节所述，使 clamped 为余因子 $h = 8$ 的倍数可防止小子群限制攻击。范围的上界 $2^{252} - 1$ 略低于 $q = 2^{252} + 27742317777372353535851937790883648493$（基点/生成元的阶）。从范围 $[0, 2^{252} - 1]$ 中选择 $k$ 在实践中等同于使用范围 $[0, q-1]$：使用后一个范围时，选择范围 $[2^{252}, q-1]$ 中的值的概率约为 $10^{-38}$。

将次高位设置为 1 的原因与余因子无关：它是帮助确保常数时间实现的预防措施。如果此比特为零，则标量乘法的实现可以在不影响结果的情况下省略 Montgomery 阶梯的第一次迭代；攻击者可以利用这种时间变化来泄露私钥的最高位（可能还有更多）。将此比特设置为一强制 Montgomery 阶梯始终使用完整的 255 次迭代。即使不将此比特设置为 1，我们的实现也是常数时间的，但 X25519 定义为始终设置此比特，作为保护不够谨慎的实现的预防措施。

钳位处理后，clamped 是 $8k$ 形式的数，其中 $k \in [2^{251}, 2^{252} - 1]$。因此，X25519 产生的群元素分布是非均匀的：阶为 $q$ 的子群中只有大约一半的群元素会被生成。然而，这种非均匀性不会显著削弱 X25519 用于 Diffie-Hellman 的安全性（除了损失 1 位熵）。

钳位处理的最后一个原因：如果 $k = 0$ 或 $k$ 是 $q$ 的倍数，则对于阶为 $q$ 的基点 $P$，我们期望 $P^k = \infty$。虽然 Montgomery 阶梯正确处理了作为中间值的无穷远点（如第 4.5 节所示），但 scalarmult 没有能力返回无穷远点，因为该函数只返回曲线点的 $x$ 坐标。如果没有钳位处理且传入了标量参数 0 或 $q$，scalarmult 将返回零，调用者无法将其与 $x$ 坐标为零的曲线点区分开来。在这种情况下返回错误会使函数非常数时间（如果不小心处理的话）。

通过强制 $k$ 在 $0 < k < q$ 范围内，钳位处理避免了需要返回 ∞ 的情况（除了攻击者提供具有小阶的群元素的情况，如第 2.7 节所讨论的，在这种情况下返回零是可以的）。在使用素数阶 $q$ 的基点进行 Diffie-Hellman 的过程中，永远不会产生 ∞，因为 $(P^j)^k = P^{jk} = \infty$ 仅当 $jk$ 是 $q$ 的倍数时成立，这只在 $j$ 或 $k$ 是 $q$ 的倍数时才可能。

## 4.8 完成标量乘法

在 scalarmult 开始时，基点的 $x$ 坐标作为参数 `point` 传入。在第 13 行，这个字节数组被转换为 field_elem 表示并复制到 x。然后我们初始化 $a = 1, b = x, c = 0$，$d = 1$。这给出了方程 (23) 中定义的 Montgomery 阶梯的起始状态：$L(P, 0) = (\frac{a}{c}, \frac{b}{d}) = (\infty, P)$。

Montgomery 阶梯完成后，a 和 c 包含曲线点 $P^{\text{clamped}}$ 的 $x$ 坐标的分子和分母。为了返回这个值，我们必须首先通过将两者相除将射影坐标转换回仿射坐标，如第 3.3 节所讨论的：我们使用 finverse 函数将 c 替换为其乘法逆元，然后使用 fmul 将 a 和 c 相乘。

最后，我们使用第 3.4 节描述的 pack25519 函数将结果的 field_elem 表示转换回 32 字节的数组，并将结果写入输出变量 out。这个值现在可以用作公钥（如果 point 是基点），或用作共享秘密来初始化对称加密方案（如果 point 是公钥或从另一用户接收的群元素），如第 4.3 节所示。

# 5 结论

本文展示了如何从基本原理逐行推导 TweetNaCl 中的 X25519 实现。从仅假设最少的数学背景知识出发，我们探索了现代常数时间密码学是如何实现的，并论证了该实现的正确性。虽然我们只研究了一个特定的实现，但其他实现（如 libsodium）与这里研究的代码共享许多原理；阅读本文后，你会发现理解其他实现的工作方式会容易得多。

作为未来的工作，将讨论扩展到椭圆曲线密码学中的其他常见算法将是很有趣的，例如 Ed25519 签名方案，以及其他曲线如 NIST 曲线和 Bitcoin 使用的 secp256k1 曲线。消除 Curve25519 余因子的 Ristretto255 素数阶群 [16] 也值得讨论。我相信通过仔细研究密码学算法的现有实现，并详细分析使其正确的原因，我们可以更深入地理解密码学，并提高密码学协议实现的质量。

# 致谢

感谢 Alastair Beresford、Daniel Hugenroth、Markus Kuhn 和 Alex Mason 对本文草稿的反馈。我感谢 Leverhulme Trust 早期职业奖学金、Isaac Newton Trust、Nokia Bell Labs 以及包括 Ably、Adrià Arcarons、Chet Corcos、Macrometa、Mintter、David Pollak、RelationalAI、SoftwareMill、Talent Formation Network 和 Adam Wiggins 在内的众筹支持者的资助。

# 参考文献

[1] [n.d.]. HACL\*, a high-assurance cryptographic library. https://hacl-star.github.io/

[2] Adrian Antipa, Daniel Brown, Alfred Menezes, René Struik, and Scott Vanstone. 2003. Validation of Elliptic Curve Public Keys. In 6th International Workshop on Practice and Theory in Public Key Cryptography (PKC 2003, Vol. LNCS, volume 2567). Springer, 211–223. https://doi.org/10.1007/3-540-36288-6_16

[3] Daniel J Bernstein. 2006. Curve25519: New Diffie-Hellman Speed Records. In 9th International Conference on Theory and Practice in Public-Key Cryptography (PKC 2006). Springer, 207–228. https://doi.org/10.1007/11745853_14

[4] Daniel J Bernstein. 2009. Cryptography in NaCl. https://cr.yp.to/highspeed/naclcrypto-20090310.pdf

[5] Daniel J. Bernstein and Tanja Lange. [n.d.]. Montgomery curves. In Explicit Formulas Database (EFD). https://hyperelliptic.org/EFD/g1p/automontgom.html

[6] Daniel J Bernstein and Tanja Lange. 2017. Montgomery Curves and the Montgomery Ladder. In Topics in Computational Number Theory Inspired by Peter L. Montgomery. Cambridge University Press, 82–115. https://doi.org/10.1017/9781316271575.005

[7] Daniel J Bernstein, Tanja Lange, and Peter Schwabe. [n.d.]. NaCl: Networking and Cryptography library. https://nacl.cr.yp.to/

[8] Daniel J Bernstein, Tanja Lange, and Peter Schwabe. 2012. The Security Impact of a New Cryptographic Library. In 2nd International Conference on Cryptology and Information Security in Latin America (LATINCRYPT 2012). Springer, 159–176. https://doi.org/10.1007/978-3-642-33481-8_9

[9] Daniel J Bernstein, Bernard van Gastel, Wesley Janssen, Tanja Lange, Peter Schwabe, and Sjaak Smetsers. 2014. TweetNaCl: A Crypto Library in 100 Tweets. In 3rd International Conference on Cryptology and Information Security in Latin America (LATINCRYPT 2014). Springer, 64–83. https://doi.org/10.1007/978-3-319-16295-9_4

[10] Daniel J Bernstein, Bernard van Gastel, Wesley Janssen, Tanja Lange, Peter Schwabe, and Sjaak Smetsers. 2014. TweetNaCl: A Crypto Library in 100 Tweets. https://tweetnacl.cr.yp.to/

[11] Ian Blake, Gadiel Seroussi, and Nigel Smart. 1999. Elliptic Curves in Cryptography. Cambridge University Press.

[12] Dmitry Chestnykh, Devi Mandiri, and AndSDev. [n.d.]. TweetNaCl.js. https://github.com/dchest/tweetnacl-js

[13] Henri Cohen, Gerhard Frey, Roberto Avanzi, Christophe Doche, Tanja Lange, Kim Nguyen, and Frederik Vercauteren. 2006. Handbook of Elliptic and Hyperelliptic Curve Cryptography. CRC Press.

[14] Craig Costello and Benjamin Smith. 2018. Montgomery curves and their arithmetic. Journal of Cryptographic Engineering 8 (2018), 227–240. Issue 3. https://doi.org/10.1007/s13389-017-0157-6

[15] Cure53, Mario Heiderich, Jonas Magazinius, and Joachim Strömbergson. 2017. Review-Report Crypto Library TweetNaCl-js. https://cure53.de/tweetnacl.pdf

[16] Henry de Valence, Jack Grigg, George Tankersley, Filippo Valsorda, and Isis Lovecruft. 2020. The ristretto255 Group. https://ietf.org/id/draft-irtfcfrg-ristretto255-00.html IETF Internet-Draft.

[17] Stefan Friedl. 2017. An elementary proof of the group law for elliptic curves. Groups Complexity Cryptology 9, 2 (Oct. 2017), 117–123. https://doi.org/10.1515/gcc-2017-0010

[18] Kazuyuki Fujii and Hiroshi Oike. 2017. An Algebraic Proof of the Associative Law of Elliptic Curves. Advances in Pure Mathematics 7, 12 (Dec. 2017), 649–659. https://doi.org/10.4236/apm.2017.712040

[19] Mike Hamburg. 2015. Decaf: Eliminating Cofactors Through Point Compression. In 35th Annual Cryptology Conference (CRYPTO 2015). Springer, 705–723. https://doi.org/10.1007/978-3-662-47989-6_34

[20] Darrel Hankerson, Alfred Menezes, and Scott Vanstone. 2004. Guide to Elliptic Curve Cryptography. Springer.

[21] Ianix.com. 2020. Things that use Curve25519. https://ianix.com/pub/curve25519-deployment.html

[22] Wesley Janssen. 2014. Curve25519 in 18 tweets. Bachelor's Thesis. Radboud University Nijmegen. http://www.cs.ru.nl/bachelors-theses/2014/Wesley_Janssen___4037332___Curve25519_in_18_tweets.pdf Archived at https://perma.cc/4HVB-D6BJ.

[23] Neal Koblitz. 1994. A Course in Number Theory and Cryptography. Springer.

[24] Adam Langley, Mike Hamburg, and Sean Turner. 2016. Elliptic Curves for Security. RFC7748. https://doi.org/10.17487/RFC7748

[25] Neil Madden. 2020. What's the Curve25519 clamping all about? https://neilmadden.blog/2020/05/28/whats-the-curve25519-clamping-all-about/ Archived at https://perma.cc/6QPY-GJLR.

[26] Moxie Marlinspike and Trevor Perrin. 2016. The X3DH Key Agreement Protocol. https://www.signal.org/docs/specifications/x3dh/ Archived at https://perma.cc/MSA4-DP4G.

[27] Peter L Montgomery. 1987. Speeding the Pollard and Elliptic Curve Methods of Factorization. Math. Comp. 48, 177 (1987), 243–264. https://doi.org/10.1090/S0025-5718-1987-0866113-7

[28] J M Pollard. 1978. Monte Carlo Methods for Index Computation (mod p). Math. Comp. 32, 143 (July 1978), 918–924. https://doi.org/10.2307/2006496

[29] Eric Rescorla. 2018. The Transport Layer Security (TLS) Protocol Version 1.3. RFC 8446. https://doi.org/10.17487/RFC8446

[30] WhatsApp. 2017. WhatsApp Encryption Overview. https://www.whatsapp.com/security/WhatsApp-Security-Whitepaper.pdf Archived at https://perma.cc/QD7M-GPG5.

[31] Jean-Karim Zinzindohoué, Karthikeyan Bhargavan, Jonathan Protzenko, and Benjamin Beurdouche. 2017. HACL\*: A Verified Modern Cryptographic Library. In ACM SIGSAC Conference on Computer and Communications Security (CCS 2017). ACM, 1789–1806. https://doi.org/10.1145/3133956.3134043

# A 与原始 TweetNaCl 的差异

本文中的代码清单基于 Bernstein 等人的 TweetNaCl 实现 [10] 的 20140427 版本。为了提高可读性，我进行了若干修改；这些修改不影响代码的功能或常数时间属性。本附录详细说明了这些变更。

- 所有不属于 X25519 实现的函数均被省略。

- 所有使用 FOR 和 sv 宏的地方均被替换为其定义。

- `gf` 类型定义被重命名为 field_elem。

- 我在运算符周围添加了空白以提高可读性。

- 在 unpack25519 函数中，参数 o 和 n 分别被重命名为 out 和 in。

- car25519 函数被重命名为 carry25519，其参数 o 被重命名为 elem，局部变量 c 被重命名为 carry。其循环体原本为：

```txt
o[i]+=(1LL<<16);
c=o[i]>>16;
o[(i+1)*(i<15)]+=c-1+37*(c-1)*(i==15);
o[i]-=c<<16;
```

向 o[i] 加上 2^16 以及随后从 o[i+1] 减去 1 的操作相互抵消，且没有明显用途，因此我将其移除。我推测加上 2^16 可能是为了确保 o[i] 非负，但事实并非如此，因为 o[i] 可能小于 -2^16，而且这也是不必要的，因为代码本身就能正确处理负值。Dan Bernstein 和 Tanja Lange 没有回复我请求澄清这些代码行的邮件。我简化后的循环体还将难以阅读的"乘以布尔值"惯用法替换为简单的 if 语句：

```javascript
carry = elem[i] >> 16;
elem[i] -= carry << 16;
if (i < 15) elem[i + 1] += carry; else elem[0] += 38 * carry;
```

对变量 i 的值进行条件分支是安全的，因为它不是秘密值（其范围始终为 0 到 15）。

- A 函数被重命名为 fadd，其参数 o 被重命名为 out。

- Z 函数被重命名为 fsub，其参数 o 被重命名为 out。

- M 函数被重命名为 fmul，其参数 o 被重命名为 out，局部变量 t 被重命名为 product。

- 我移除了 S（平方）函数，它本来就只是调用 M，并将其替换为直接调用 fmul。

- inv25519 函数被重命名为 finverse，其参数 o 和 i 分别被重命名为 out 和 in，局部变量 a 被重命名为 i（因为用作循环计数器）。

- sel25519 函数被重命名为 swap25519，其参数 b 被重命名为 bit。

- 在 pack25519 函数中，参数 o 和 n 分别被重命名为 out 和 in，局部变量 b 被重命名为 carry。

- crypto_scalarmult_base 函数被重命名为 scalarmult_base，其参数 q 和 n 分别被重命名为 out 和 scalar。

- crypto_box_keypair 函数被重命名为 generate_keypair，其参数 y 和 x 分别被重命名为 pk 和 sk。

- 添加了 x25519 函数作为 scalarmult 的别名。

- crypto_scalarmult 函数被重命名为 scalarmult，其参数 q、n 和 p 分别被重命名为 out、scalar 和 point，局部变量 z 和 r 分别被重命名为 clamped 和 bit。第一个循环的上界从 31 改为 32，随后的两行（执行钳位处理）被轻微重构以使代码更清晰，但不改变其行为。我将变量 x 从原本的 80 元素 i64 数组改为 field_elem 类型（即 16 元素的 i64 数组）。x 的前 16 个元素具有与原来相同的功能（即输入点坐标的内部表示）。原始代码在完成 Montgomery 阶梯后，将局部变量 a 的值复制到 x 的索引 16-31，c 复制到索引 32-47，b 复制到索引 48-63，d 复制到索引 64-79，然后对变量 x 的偏移量执行最后三个函数调用（c 的求逆、a 与 c 的乘法，以及结果的 pack25519）。将 a、b、c 和 d 复制到 x 的目的不明确，因为没有任何地方使用了这些值已被复制到一个连续数组中的事实。也许是早期调试逻辑的残留？Dan Bernstein 和 Tanja Lange 没有回复我请求澄清为何如此的邮件。因此我移除了复制到 x 的操作，并将 scalarmult 的最后三个函数调用改为直接操作 a 和 c。

我使用 NaCl 包 [7] 提供的测试向量运行了修改后的代码，并验证它们计算出了相同的结果。
